# 3D 水族馆场景 · 技术方案

> 目标：把 Khronos webglsamples Aquarium 的 3D 鱼缸场景接入 Cocos / Laya，测 2.5D 路线的 3D 渲染能力。
> Egret 5.x 不支持 3D，不参与（SPEC 已明确）。

---

## 一、原版资源盘点（已实查）

### 1. 鱼模型（5 鱼种）
`aquarium/assets/*.js`，JSON 结构：
```
{ models: [ { textures: {diffuse, normalMap}, fields: {
    position: Float32Array (456顶点×3)
    normal:   Float32Array (456×3)
    texCoord: Float32Array (456×2)
    tangent:  Float32Array (456×3)
    binormal: Float32Array (456×3)
    indices:  Uint16Array (152三角面×3)
} } ] }
```
5 鱼种：SmallFishA / MediumFishA / MediumFishB / BigFishA / BigFishB
贴图：`*_DM.jpg`（漫反射）+ `*_NM.png`（法线）+ `*_RM.jpg`（反射，部分有）

### 2. 场景资源
- 水族箱背景、岩石（RockA/B/C）、海草（SeaweedA/B）、沉船（SunknShip）、珊瑚等 —— 均为 `.js` 内嵌网格 + 贴图
- 天空盒（Skybox + GlobeOuter_EM_* 6 面 cubemap）
- 地板（FloorBaseA）

### 3. 鱼渲染机制（aquarium.js 源码）
- **游动**：Lissajous 轨道（fishXClock/fishYClock/fishZClock 三个正弦时钟）
- **鱼身弯曲**：顶点 shader 用 fishLength/fishWaveLength/fishBendAmount 做顶点级弯曲
- **光照**：法线贴图 + 反射 + 雾

---

## 二、两引擎 3D 能力对照

| 能力 | Cocos 3.8.8 | Laya 3.4 |
|---|---|---|
| 网格加载 | Mesh + MeshRenderer | Mesh3D + MeshRenderer |
| 材质 | 内置 PBR/unlit 材质 | PBR/BlinnPhong |
| 法线贴图 | ✅ | ✅ |
| 顶点动画（鱼身弯曲） | 需自定义 shader 或骨骼 | 需自定义 shader 或骨骼 |
| 天空盒/环境 | ✅ | ✅ |
| 3D 编辑器搭建 | ✅ 强 | ✅ 强 |

---

## 三、实现路线（分阶段）

### 阶段 1：静态 3D 鱼（最小可跑）
- 解析鱼模型 JSON → 建 Mesh → 贴 diffuse 贴图 → 场景里显示一条鱼
- 验证：Cocos/Laya 各渲染出一条鱼

### 阶段 2：鱼游动（Lissajous 轨道）
- 移植原版轨道运动到两引擎（CPU 侧更新鱼 transform）
- 验证：5 鱼种按轨道游动

### 阶段 3：鱼身弯曲（顶点动画）
- 两引擎写顶点 shader（fishBendAmount 弯曲），或简化为"整体摆动"
- 这是最复杂的部分，可后续迭代

### 阶段 4：完整场景
- 背景/岩石/海草/天空盒/光照
- 鱼数量梯度（50/100/200/400）测承载力

---

## 四、关键决策点（需用户确认）

1. **渲染范围**：先做"鱼 + 简单背景"还是"完整鱼缸场景"？
   - 建议：先鱼 + 纯色/渐变背景（最快出数据），完整场景后补
2. **鱼身弯曲**：顶点 shader（忠实原版，工作量大）vs 整体摆动（快，但少了原版的"鱼身波动"）
   - 建议：先整体摆动，弯曲后续迭代
3. **两引擎谁先**：Laya 3D 是强项（WebGPU 宣传点就是 3D），Cocos 3D 也成熟
   - 建议：并行，都用"编辑器搭建 + 脚本采集"模式

---

## 五、与 2D 水族馆的关系

- 2D 水族馆：测 2D 精灵渲染 + 多纹理合批（已完成）
- 3D 水族馆：测 3D 渲染管线 + 三角形数 + 3D 专用指标（triangle count）

两者互补，共同回答"2D 路线 vs 2.5D/3D 路线"的选型问题。
