# 自研 Egret 引擎魔改思路总结

> **对象**：自研 Egret 5.4.1（游戏引擎 + WebGPU 后端 = 完整自研形态）
> **用途**：① 选型对比测试的理论底稿——知道我们有什么，才能测出"最大性能"；
> ② 后续维护/复测的核对清单
> **来源**：`dev_sdf20260806/h5client_s2`（游戏引擎）+ `egret-webgpu/webgpu-dist`（WebGPU 后端）
> 源码级实查，非记忆复述。日期：2026-08-27

---

## 〇、架构分层（先理清三个引擎副本）

| 副本 | 位置 | 内容 |
|---|---|---|
| 自研基线 | `egret-selfdev/5.4.1/build` | 原生 Egret + 基线魔改（动态合图全套等） |
| 游戏引擎 | `h5client_s2/libs/modules/egret` | 基线 + **SDF 文字渲染**（2026-08-25 上线） |
| WebGPU 重编版 | `bench/engines/egret-webgpu/libs` | **裁剪版** + webgpu 分支（6_21 实验产物）⚠️ 缺基线魔改 |

**⚠️ 基准测试现状**：我们目前测的 Egret 用的是"WebGPU 重编版"——它有 WebGPU 分支，
但**动态合图等基线魔改被裁掉了**。所以 Egret WebGL 臂没有发挥最大性能（V2/V4 破批
9999 dc 正是"没合图"的表现）。完整形态 = 游戏引擎 + webgpu 分支，见文末行动项。

---

## 一、基线魔改（WebGL，自研 5.4.1 自带）

### 1. 动态合图系统（跨图片合批的 WebGL 方案）
- `TextureAtlasManager`：2048×2048 自动大图集 × 最多 10 张；≤512×512 纹理自动入驻；
  网格分配（`getGirdID`）、纹理移除/更新、debug 可视化节点
- `BatchType` 逐纹理控制：`Batch`(入图集) / `Disable`(强制不合批，遮罩自动降级) /
  `Ignore` / `IgnoreSelf`——业务用后两者排除 UV 改写类内容（模型 MovieClip 打
  `IgnoreSelf`、Spine 打 `Disable`）
- 开关：`egret.sys.openAutoBatch`（**生产默认 false**）+ `openBatchTextureCmd`（命令级
  合批，开发工具可运行时切换）
- **停用原因推断**：UV 改写冲突（SpinePanel 注释"UV 修改导致合批异常"）+ 图集容量约束

### 2. 其它基线魔改
- `etc_alpha_mask` 双纹理：一张图 + 一张 alpha 掩码合为一次绘制（TEXTURE1 通道）
- `textureSharpen` 纹理锐化 program
- `colorTransform` 颜色变换路径（27 处）
- 性能基建：`startGetPerformance` 官方统计区间、`profileWebGLTexture` 纹理画像、
  `disableGLClear`、渲染逻辑分层开关、`textBatchIgnoreSelf`
- 压缩纹理兼容：`checkCompressTextureInternalFormat`（处理微信安卓 ASTC 6x6 特例）

## 二、SDF 文字渲染（游戏引擎实装）

- **路线演进**（踩坑记录，全部源码可查）：离线烘焙（改字重烤+2x 超采样+缓存复杂）→
  运行时 Mesh+CustomFilter（FBO UV 错、污染兄弟 UI）→ **引擎层 sdf_text（唯一正确路线）**
- **机制**（对标 Godot CANVAS_RECT_MSDF）：atlas BitmapData 打 `isSDF` 标志 →
  `getWebGLTexture` 同步 GL 纹理 → `drawData` 分支切 `sdf_text` program（注入
  uPxRange/uBoldness/uOutlineWidth 等 uniform）→ 逐字形 drawImage 走**标准批次器**
- **合批特性**：同图集+同样式合批；颜色走顶点 tint 不断批；fwidth 自适应 AA +
  gamma + 描边挖空 over
- **动态补字**：`SDFDynamicAtlas` 运行时烘字入追加图集，`texSubImage2D` 增量上传
- **关键坑**（复用价值高）：预乘混合输出、描边环外扩防 bold 遮盖、AA 乘数 1.5、
  bold 粘黏上限、mangle 白名单、双份引擎维护点（login.js 与 egret.web.js）
- **对齐方法论**：RenderTexture 逐像素 diff 探针量化（不靠肉眼）

## 三、WebGPU 后端优化全清单（webgpu-dist，不止合批）

### 渲染核心（egret-webgpu.js + batcher.js）

| # | 优化 | 机制 | 效果 |
|---|---|---|---|
| 1 | **多纹理合批** | 一批 ≤16 纹理槽（对齐 Pixi/Laya 上限），texId 写进顶点（9 float 格式：pos2+uv2+color4+texId1） | 破批场景 dc 9999→1，帧时间 -40%+（基准实测） |
| 2 | **变换烘顶点** | 旋转/缩放/斜切矩阵烘进顶点坐标 | V3 变换场景不断批 |
| 3 | **颜色走顶点 tint** | 顶点色乘法（Godot modulate 同构） | 颜色各异不断批 |
| 4 | **帧级 staging 上传** | 整帧顶点/索引攒 CPU 大数组，帧末**一次** writeBuffer（替代每批 2 次，1000+ 批 = 2000+ API 调用 → 1 次） | 显著降提交/驱动开销（Metal/Safari 小 writeBuffer 尤贵） |
| 5 | **状态去冗余** | 连续相同状态跳过 setPipeline/setBindGroup（对齐 Cocos/Laya 状态去冗余）；VB/IB 整帧绑一次（`_vbufBound`） | 省重复状态设置 |
| 6 | **BindGroup 缓存** | `WeakMap(tex → BindGroup)` + 绑定签名缓存 | 免重复构建绑定 |
| 7 | **Stencil 懒路径** | 无任意形状 mask 时默认关 depth/stencil；mask 管线按 op 缓存 | 省移动 GPU 每帧成本 |
|  计时** | timestamp-query **每 60 帧采一次**（该查询在 Safari/Metal 开销高，防测量污染 FPS） | 可得真实 gpuMs 而不失真 |
| 9 | SDF 模块（WebGPU 版）+ 后处理模糊管线（懒建） | 独立 pipeline/layout/buffer 组 | WebGPU 上延续文字/特效能力 |
| 10 | 白纹理兜底 + multiTex 可关 | 纯色矩形走 1×1 白纹理 slot0；可退回单纹理老逻辑 | 兼容性/对照测试 |

### 工程设计（同样重要）

- **桥接层（egret-bridge.js）**：只依赖 egret 公开 `$` 前缀契约；纯逻辑可 mock 单测；
  RenderNodeType 常量兜底（const enum 编译擦除）
- **适配器韧性（egret-webgpu-adapter.js）**：adapter 四级降级请求
  （high-performance → 默认 → low-power → forceFallbackAdapter）；压缩纹理按能力请求
  （有才要，防 requestDevice 失败）
- **纯 CPU 合批核心（batcher.js）**：无 GPU 依赖可单测；6_21 面板的 drawCall/submitMs
  探针都挂在这层

### GPU compute 能力原型（超越 2D 渲染的储备）

- **gpu-school.js**：游戏内"鱼群风暴"特效层——**10 万鱼 Boids 物理+渲染全 GPU**，
  三段 compute（空间哈希建网格 → 求力 → 积分），GPU/CPU 同屏 A/B 对照
  （WebGL 无 compute 的现实对照）
- **gpu-coins.js**：同模式的金币特效层
- 这正是"WebGPU 对 2D 的引擎投入型收益"清单里 compute 粒子/群集的已验证原型

---

## 四、与 Laya/Cocos 的对照定位

| 能力 | Egret 自研 | Laya 3.4 | Cocos 3.8.8 |
|---|---|---|---|
| 同纹理合批 | ✅ 原生批次器 | ✅ | ✅ |
| 动态合图（跨图） | ✅ 有系统，默认关 | ✅ 自动合图（默认开） | 部分（资源管线图集） |
| SDF 文字 | ✅ 引擎层已上线 | ❌ | ❌ |
| 多纹理合批（WebGPU） | ✅ 16 槽 | ❌（textureArrayIndex 未接线） | 未知（待基准验证） |
| GPU compute 特效 | ✅ 原型（10 万鱼） | 3D 向 | 3D 向 |
| 双纹理掩码 | ✅ etc_alpha_mask | ✅ | ✅ |

## 五、基准测试行动项（发挥最大性能）

1. **Egret WebGL 臂换完整基线引擎**（`egret-selfdev/5.4.1/build`，含动态合图），
   并开 `egret.sys.openAutoBatch = true`——量化"动态合图"这代方案的威力
2. **WebGPU 臂**维持现引擎（多纹理槽合批）——两代合批方案同台对比
3. （进阶）把 webgpu 分支移植进游戏引擎 → 终极形态"完整引擎双后端"
4. 测试报告标注：Egret 为自研引擎，上述优化为本团队开发（利益相关披露）
