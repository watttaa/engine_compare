# Cocos Creator vs LayaAir 引擎评估汇报

> 依据：《引擎选型对比报告》+《三引擎2D渲染管线对比》+《WebGPU-2D渲染现状调研》+ 本地统一基准实测数据
> 基线版本：Cocos Creator 3.8.8 / LayaAir 3.4 · 汇报日期：2026-08-31

---

## 一、结论一句话

**面向 2D/2.5D 重度 H5 项目（Web + 微信 + 抖音三端），推荐 Cocos Creator 3.8.x。**
LayaAir 的差异化优势在「重度 3D + WebGPU 上限」，与我们以 2D 为主的品类诉求不匹配；
且我们在统一基准上的实测发现：**Laya 2D 在多纹理/变换密集场景存在系统性性能短板，不是单点问题。**

---

## 二、我们做了什么（方法简述）

1. **选型维度调研**：2D/3D 能力、平台适配（微信/抖音）、包体启动、编辑器工具链、生态社区、商业授权、案例、风险，共 11 个维度逐项对比。
2. **源码级实查**：本地拉取两引擎源码（含我们自研 Egret 引擎作第三参照），逐条核对 2D 合批路径、变换路径、WebGPU 提交路径。
3. **统一基准实测**：同机、同分辨率、同资源、同采集代码（sim-core），BunnyMark V1-V4 四个 2D 压力场景（10000 精灵）+ Boids 群集场景，WebGL / WebGPU 双后端。

---

## 三、实测数据（BunnyMark · 10000 精灵 · 真机）

| 场景 | 含义 | Cocos WebGL/WebGPU | Laya WebGL/WebGPU |
|---|---|---|---|
| V1 单纹理 | 合批饱和基线 | 57.7 / 58.1 | 58.2 / 58.2（打平） |
| V2 8 纹理交叉 | 图集帧混排（真实项目常态） | **58.3 / 58.3** | **29.4 / 33.1** |
| V3 单纹理+变换 | Buff/飘字类动态特效 | **58.6 / 57.3** | **43.3 / 41.4** |
| V4 12 纹理交叉 | UI 混排 | **58.2 / 58.1** | **38.0 / 29.8** |

补充指标：
- **DrawCall（本次交叉散图基准）**：Cocos 全场景 dc≈10；Laya 在 V2/V4（未启用动态图集的交叉散图路径）dc=10000（=精灵数）。这说明该测试配置未发生跨纹理合批，不等同于 Laya 2D 的同纹理/同图集路径完全不合批。
- **掉帧率**（帧时间>16.7ms 占比）：Cocos V2 约 47%（半数帧轻微超预算）；**Laya V2/V3/V4 为 100%（每一帧都慢）**。
- **V3 上 Laya 的 WebGPU 反而比自家 WebGL 更慢**，说明其 WebGPU 2D 路径有额外开销。

**一句话读数据：单纹理场景两者打平；一旦纹理交叉或变换密集（这正是 MMO 战斗/特效/复杂 UI 的常态），Laya 帧率掉到 Cocos 的一半左右。**

---

## 四、差距的源码级根因（三条独立机制叠加）

1. **合批策略**
   - Cocos：Batcher2D 状态机 + `WEBGL_multi_draw` 扩展，跨纹理也不断批 → dc≈10（三家唯一）。
   - Laya：普通 Sprite 走 `NullBatchProvider`，**逐个精灵独立提交，dc 直接等于精灵数**；纹理阵列合批的字段（`textureArrayIndex`）有读取代码但全引擎零赋值——架子搭了没接线。

2. **变换路径**
   - Cocos：变换在合批提交时批量处理。
   - Laya：`Matrix.setMatrix` 每帧每精灵无条件重算 6 次三角函数（skew=0 也算）→ 1 万精灵每帧 6 万次三角函数 + 完整矩阵重建。

3. **WebGPU 提交方式**
   - Cocos：WebGL/WebGPU 同源管线（GFX 抽象），无额外负担。
   - Laya：WebGPU 路径每帧对每个精灵跑一遍 `_prepare` 状态机（矩阵检查 + shader 比较 + 几何状态），开销随精灵数线性增长——这就是它 WebGPU 反而更慢的原因。

**结论：Laya 2D 表现差是三处机制叠加的系统性结果，不是某个开关没开。**（其动态合图/多纹理合批不是没开，是 3.4 版本没实现。）

官方口径也印证：Laya 官方文档把 WebGPU 收益明确圈定在「3D 渲染、复杂后处理、GPU 计算」，只字未提 2D。

---

## 五、选型维度对比要点（除性能外）

| 维度 | 优势方 | 关键理由 |
|---|---|---|
| 2D 工作流（合批/图集/2D光照/粒子/Tilemap） | **Cocos** | 工作流级能力全面领先 |
| 微信小游戏 | **Cocos** | 微信官方内置 Cocos 引擎插件，首包 4MB 几乎不用装引擎 |
| 抖音小游戏 | **Cocos** | 字节是 Cocos 股东，新特性首发适配 |
| 编辑器/动画状态机/CI 构建/插件商店 | **Cocos** | 企业级工程能力折算人力=每年数人月成本差 |
| 生态/社区/招聘 | **Cocos** | 中文社区体量约为 Laya 的 5~10 倍，人才池数倍 |
| 重度 3D / WebGPU 上限 | LayaAir | 3D 管线投入激进，但非本项目核心诉求 |
| 引擎源码可改性 | LayaAir | 全 TS 实现，改引擎不碰 C++ |
| 同品类历史案例 | **Cocos** | 《咸鱼之王》《寻道大千》验证"重度长线运营+双端小游戏"完整链路；Laya 的 MMO 案例集中在 2016-2019 的 1.x/2.x 时代 |

商业授权两者均为 MIT 免费、无流水分成，成本层面平手。

---

## 六、风险与对策

**选 Cocos 的主要风险（可控）：**
- 4.0 大版本重构期的迁移 → 锁定 3.8 LTS，4.0 稳定后再评估。
- 引擎级小缺陷（例：3.8.8 WebGL 构建 `numDrawCalls` 统计恒 0，官方性能面板 drawCall 不可信）→ 我方自建统计口径，不依赖引擎面板。

**若选 Laya 需额外承担：**
- 2D 性能短板需靠资源管线（强制同图集）部分规避，且受美术管线约束，无法根治；
- 生态小 → 疑难问题无人踩过坑，需自建引擎组（+1~2 名引擎向工程师）；
- 招聘池小，团队扩张摩擦高。

---

## 七、建议

1. **确定 Cocos Creator 3.8.x** 作为项目引擎，锁定最新 patch 版本，建立升级评审机制。
2. 首包策略：微信引擎插件 + 首场景 <2MB + Asset Bundle 全远程，目标"点开到可玩 <5 秒"。
3. Demo 期定死资源规范（纹理压缩格式、图集分包、Spine 规格），并跑低端机内存/帧率基线。
4. 早接 CI：命令行构建 + 三端每日自动出包。
5. 持续跟踪：用我们的统一基准在 Laya 3.5（WebGPU Driven 路线图）发布后复测，保留再评估的客观依据。

---

## 附：数据可信度与局限

- 所有实测为同一台真机、同分辨率、同采集代码（预热 3s + 采样 10s），双方可开优化均已开启（UBO、multi_draw 自动检测等），不存在"没开优化"的不公平对比。
- DrawCall 口径：Cocos 为 GPU 提交数，Laya 为合批后渲染元素数，均已标注。
- 局限：以 2D 精灵压力场景为主，骨骼动画（Spine）与 3D 混合场景尚需补测；结论以选型前垂直切片验证（登录→场景→战斗→分包加载）为最终确认。

---

## 附录 B：引擎层架构差距（本地源码实查）

> 基线：cocos-engine 3.8.x 源码 / LayaAir 3.4 产物（bench/web/vendor）。本节回答"除了功能清单，两个引擎在架构层的代差在哪"。

### B1. 图形设备抽象层（Cocos GFX"虚拟设备"）——差距的总根源

- **Cocos**：所有渲染代码（含 2D 的 Batcher2D）只 import GFX 的 `Device/Buffer/Texture` 抽象，从不直接碰 `gl.*`。GFX 之下有 webgl / webgl2 / webgpu / empty 四套后端，原生端另接 C++（Vulkan/Metal/GLES）。源码实锤：`cocos/2d/renderer/batcher-2d.ts` 只依赖 `../../gfx`。
- **Laya**：2D 是**两份独立实现**——`laya.webgl_2D.js`（200KB）与 `laya.webgpu_2D.js`（290KB），两套代码各自演化。源码实锤：`_updateMatChangeFlag` 逐元素状态机**只存在于 WebGPU 版**。
- **推论**：
  1. Cocos 换后端行为一致（我们实测 WebGL/WebGPU 的 dc 都是 ≈10、fps 差 <1）；Laya 两后端行为已经分叉——这正是"WebGPU 比自家 WebGL 更慢"的架构级根因；
  2. Cocos 修一处、全后端受益（multi_draw 一处实现通吃）；Laya 每加一个后端 = 重写一份 2D 渲染器，维护双倍；
  3. 新图形 API 出现时，Cocos 只需在 GFX 层加实现，2D/3D 上层零改动。

### B2. 2D 渲染器的架构位置

- **Cocos**：Batcher2D 是统一状态机，2D 节点/UI/粒子都汇进同一条合批路径，再经 GFX 提交。合批判定（同材质/同混合/同纹理源/连续层级）集中在一处，可被 multi_draw 扩展整体加速。
- **Laya**：struct 按 zIndex 分层 → 批处理器注册表（只注册了序列帧一种）→ 未注册类型逐个透传。合批能力分散且覆盖面窄，这就是 dc=精灵数 的由来。

### B3. 底层语言与性能天花板

- **Cocos**：TS 业务层 + C++/WASM 底层（物理、Spine 运行时、原生渲染），3.8.5+ 支持 WASM 模块分离、按需加载（不占小游戏主包）。
- **Laya**：全 TS 引擎，读改源码门槛低（前文已述），但热点路径（如 `Matrix.setMatrix` 每帧 6 次三角函数）没有更低层的高速实现可下沉。

### B4. 内存与对象工程

- 两家都有对象池（实测源码：Cocos `CachedArray/RecyclePool/Pool`，Laya `Matrix.recover→Pool`），此项打平。
- 差异在小游戏内存策略：Cocos 的 WASM 分离 + 模块按需加载是官方工程项；Laya 全 TS 对象进 JS 堆，差异在 MMO 业务内存面前占比小，但方向上 Cocos 更可控。

### B5. 引擎自带统计的可信度（双方都有坑，我方已自建口径）

- Cocos 3.8.8 WebGL 构建 `device.numDrawCalls` 恒 0（命令缓冲从不递增）——官方性能面板 drawCall 不可信；
- Laya 的 `CT_2DDrawCall = 渲染元素数`，破批场景下等于精灵数，口径与 GPU 真实提交数不同；
- 结论：两家的面板数据都不能直接横向对比，我们的基准统一走自研采集（这也是为什么要建统一基准）。
