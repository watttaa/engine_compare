# 三引擎 2D 合批机制深度对比

> 基于 Egret 自研 5.4.1 / Cocos Creator 3.8.8 / LayaAir 3.4.0 源码实测  
> 场景：BunnyMark 10000 精灵，GTX 1650 SUPER，Chrome HeadlessChrome 152

---

## 1. 结论先行

| 引擎 | 合批策略 | V1（1纹理） | V4（12散图） | 核心机制 |
|---|---|---|---|---|
| **Egret WebGL** | CPU 动态合图 | dc=5 | dc=5（相同） | TextureAtlasManager 全自动 |
| **Cocos WebGL** | VBO 顺序合并 | dc=10 | dc=10（相同） | Batcher2D + MeshBuffer 固定槽 |
| **Laya WebGL** | 按纹理状态合批 | dc=2 | dc=**10000** | V4 未启用动态图集，交叉散图逐精灵断批 |
| **Egret WebGPU** | 批处理器计数 | dc=1 | dc=1 | 单 drawcall 全批 |
| **Laya WebGPU** | 按纹理状态合批 | dc=2 | dc=**10000** | V4 未启用动态图集，交叉散图逐精灵断批 |

**帧时（p50）对比（10000只，WebGL）：**

| 引擎 | V1 p50 | V4 p50 | V4 fps |
|---|---|---|---|

| Egret | 17ms | 17ms | 60 |
| Cocos | 16.67ms | 16.68ms | 58.9 |
| Laya | 18.1ms | **33ms** | **29.8** |

**Laya V4 帧时是 Egret/Cocos 的 2 倍；在本站未启用动态图集的交叉散图路径中，dc=10000 是主要观察到的关联因素。**

---

## 2. DrawCall 的本质

GPU 是一个流水线处理器，CPU 每次调用 `gl.drawElements()` 或 `gl.drawArrays()` 都是一次 **DrawCall（GPU 绘制指令）**。每次 DrawCall 有固定开销：

```
CPU 侧：组装参数、绑定纹理、上传 uniform、发送指令
GPU 侧：接收指令、切换渲染状态、执行顶点/片元着色器
```

1 帧 10000 次 DrawCall vs 5 次 DrawCall 的差距，**主要体现在 CPU 侧的调度开销和 GPU 状态切换**，而不仅是着色器计算量。

---

## 3. Egret：TextureAtlasManager 动态合图

### 3.1 核心原理

Egret 的关键洞察：**合批的前提是"同一 GL 纹理"，与图片内容无关**。

`TextureAtlasManager` 在运行时把多张散图自动打包进同一张大图（Atlas），让 GPU 眼里只有一张纹理。

```
12 张兔子散图（各自独立 GL 纹理）
  ↓  TextureAtlasManager（openAutoBatch=true）
一张 2048×2048 Atlas（单一 GL 纹理）
├── 区域A: rabbitv3.png      UV: (0,0)~(26,37)
├── 区域B: rabbitv3_ash.png  UV: (26,0)~(52,37)
├── 区域C: rabbitv3_batman.png ...
└── ...12 张全部打进来

所有精灵引用同一 GL 纹理 → 无纹理切换 → batcher 可全部合批
```

### 3.2 关键源码

```javascript
// egret.web.js:7929 — createTexture 时拦截，自动打进 Atlas
if (egret.sys.openAutoBatch && batch) {
    var tex = web.TextureAtlasManager.createTexture(this, bitmapData);
    if (!tex) {
        tex = egret.sys.createTexture(this, bitmapData, premultiplyAlpha, glFormat, type);
    }
    return tex;
}

// egret.js:8821 — uriValue 由 bd.url 的 hashCode 生成（每张图唯一）
if (egret.sys.openAutoBatch) {
    this.$uriValue = egret.NumberUtils.convertStringToHashCode(value);
}

// TextureAtlas.addTexture — 按 uriValue 查缓存，已在 Atlas 里则复用
var id = TextureAtlasManager.getGirdID(bitmapData["uriValue"]);
var targetGrid = this.getGrid(id);
if (!targetGrid) {
    var result = this.binPacker.insert(id, bitmapData.width + padding*2, ...);
    // 装进大图
}
```

### 3.3 为什么 V4 dc 仍是 5 而不是 1？

**Egret batcher 有 VBO 大小限制：每次最多提交 2048 个 quad（精灵）**。

```javascript
// egret.web.js:213
this.maxQuadsCount = 2048;
```

10000 精灵 / 2048 = ceil = **5 次 flush = dc=5**。

这与纹理数量无关（V1 单纹理也是 5），说明 Atlas 合图完全成功，dc 数量仅由 VBO 容量决定。

### 3.4 使用条件

```javascript
// 零代码即可开启，引擎默认行为
egret.sys.openAutoBatch = true;  // 默认值

// 唯一要求：散图加载时设置唯一 url（运行时动态加载必须手动设）
var bd = new egret.BitmapData(img);
bd.url = img.src;  // 这一行不能省，否则 uriValue = undefined → 合图 grid 冲突
```

---

## 4. Cocos：Batcher2D + VBO 顺序合并

### 4.1 核心原理

Cocos 3.x 的 `Batcher2D` 遍历场景树时维护当前批次状态（`_currTexture`、`_currMaterial`），相邻精灵如果状态完全一致就追加到同一个 MeshBuffer，最终一次 DrawCall 提交。

**合批断裂条件（源码 `batcher-2d.ts:commitComp`）：**

```typescript
if (this._currHash !== dataHash           // renderData 变了
    || dataHash === 0
    || this._currMaterial !== mat          // 材质变了
    || this._currDepthStencilStateStage !== depthStencilStateStage) {
    // flush 当前 batch，开新 batch
    this.autoMergeBatches(this._currComponent!);
    ...
    this._currTexture = frame.getGFXTexture();   // ← 纹理切换也会导致此处刷新
}
```

### 4.2 为什么 V1/V4 都是 dc=10？

Cocos 的 `StaticVBAccessor` 管理 MeshBuffer，每个 buffer 有固定大小。**10000 精灵填满若干 buffer → 自动 flush → dc ≈ 10**。

实测：dc 与纹理数量无关（V1 单纹理 dc=10，V4 十二纹理 dc=10），说明 Cocos 也对多纹理散图做了某种处理（动态 Atlas 或合并）使纹理切换不破坏批次，真正的 flush 触发点是 buffer 容量。

> **注**：测试中 Cocos 的 `__ccGlProbe` 探针确认每帧 ~10 次 `gl.drawElements` 调用，与 Cocos 官方 Stats 面板的 DrawCall 数一致。`WEBGL_multi_draw` 扩展存在于产物中，但 2D Sprite 场景实际 dc 数来自 VBO 批次而非扩展。

### 4.3 Cocos 的 Dynamic Atlas

Cocos 有运行时 [Dynamic Atlas](https://docs.cocos.com/creator/3.8/manual/zh/advanced-topics/dynamic-atlas.html)（类似 Egret TextureAtlasManager）：

```typescript
// project.json 开启
"dynamicAtlas": { "enabled": true, "maxFrameSize": 512 }
```

开启后小图会自动合入 Atlas，纹理切换减少。本次测试**未开启**（默认 project.json），Cocos 的 dc=10 是 VBO 批次而非纹理组数——说明即使不依赖 Dynamic Atlas，Cocos 的 Batcher2D 也能维持稳定的低 dc。

---

## 5. LayaAir：按纹理状态合批

### 5.1 核心原理

LayaAir 3 的 `WebGraphicsBatch` 合批条件（源码 `WebGraphicsBatch.ts:isCompatible`）：

```typescript
// 只要 textureKey 不同，立刻返回 false
let elementTexId = element.textureKey & (~defineMask);
if (elementTexId !== 0 && elementTexId !== this.textureId && this.textureId !== 0)
    return false;
```

`textureKey` = 原始 GL 纹理 ID + shader defines。**不同散图 → 不同 textureKey → 不可合批**。

### 5.2 为什么 V2/V4 dc = 10000？

```
10000 只精灵，V4 用 12 张散图（i % 12 轮换）：
  精灵0: rabbitv3.png      (textureKey=A)
  精灵1: rabbitv3_ash.png  (textureKey=B)  ← A ≠ B，flush
  精灵2: rabbitv3_batman.png (textureKey=C) ← B ≠ C，flush
  ...
每只精灵和相邻的不同 → 每帧 10000 次 flush = dc=10000
```

### 5.3 合批成功的条件（V1/V3）

V1（单纹理）：所有精灵 textureKey 相同 → **全部合批为 1 批**（dc=2，剩余 1 次为引擎其他固定开销）。

V3（rotation+scale+单纹理）：textureKey 仍相同 → dc 仍为 2。帧时变差（fps 降至 37.5）是因为 **CPU 端每帧重新计算变换矩阵**的开销，不是合批问题。

### 5.4 LayaAir 有没有动态合图？

有两条路径：

1. **显式动态图集**：可创建 `DynamicAtlasManager` 并注册纹理；
2. **自动动态图集**：源码 `LargeTexProcessor.ts` 在 `AutoTextureConfig.enableAutoDynamicAtlas` 为真时自动创建 `DynamicAtlasManager`，对尺寸小于 `limitDynamicAtlasSize` 的 `Texture2D` 调用 `addTexture`。

本站 V2/V4 适配器的交叉散图路径**未启用或验证自动动态图集**，故测到的是原始纹理交叉时的标准 Sprite 提交行为；不能外推为 LayaAir 2D 在所有配置下不合批。

---

## 6. 三引擎合批能力横向对比

### 6.1 技术路线对比

```
Egret TextureAtlasManager
─────────────────────────
散图A ──┐
散图B ──┤ CPU打包 → Atlas大图 → 1张GL纹理 → Batcher合批
散图C ──┘
         ↑
         不切换纹理 → VBO容量是唯一限制 → dc = ceil(N/2048)

Cocos Batcher2D
───────────────
散图A        散图B        散图C
  ↓            ↓            ↓
材质相同? → 追加MeshBuffer → Buffer满时flush
         → dc = buffer满的次数（约 N/1000）

LayaAir Sprite 渲染（未启用动态图集的交叉散图路径）
──────────────────
散图A → textureKey=A → Batch#1 → draw()
散图B → textureKey=B → Batch#2 → draw()  ← 纹理不同直接断批
散图C → textureKey=C → Batch#3 → draw()
         ↑
         dc = 纹理切换次数（极端情况 = 精灵数）
```

### 6.2 性能影响（MMO 典型场景）

**场景：100 个不同职业角色同屏（每人不同贴图）**

| 引擎 | dc 预估 | 帧时影响 |
|---|---|---|
| Egret | ceil(100/2048) = **1** | 几乎无额外开销 |
| Cocos | ceil(100/1000) = **1** | 几乎无额外开销 |
| Laya | **100**（每人一次draw） | 随角色数线性增长 |

**场景：1000 个飘字/弹幕（大量不同纹理）**

| 引擎 | dc 预估 |
|---|---|
| Egret | ceil(1000/2048) = **1** |
| Cocos | ceil(1000/1000) = **1** |
| Laya | **1000** |

### 6.3 WebGPU 对合批的影响

| 引擎 | WebGPU dc（V4 10000只）| 原因 |
|---|---|---|
| Egret WebGPU | **1** | 批处理器 `_lastBatchCount`，全帧一次提交 |
| Cocos WebGPU | **10** | 同 WebGL，buffer 批次 |
| Laya WebGPU | **10000** | 同 WebGL，纹理匹配合批，散图无法合批 |

LayaAir 的交叉散图问题在 WebGPU 下同样存在；这描述的是本站未启用动态图集的标准 Sprite 测试路径，不应外推为引擎所有 2D 配置。

---

## 7. 选型意义

### 对 MMO 游戏的实际影响

1. **角色系统**：MMO 通常有大量不同贴图的角色同屏。Egret 可通过 TextureAtlasManager 自动合图；Laya 在未启用自动动态图集时需显式管理动态图集或接受交叉散图带来的高 DrawCall。

2. **UI 混排**：装备图标、道具栏、buff 图标等大量不同小图混排。Egret/Cocos 可通过 Atlas 或 buffer 合批；Laya 的未合图散图路径会出现高 DrawCall。

3. **开发成本**：
   - Egret：`openAutoBatch=true` 自动动态合图路径
   - Cocos：Batcher2D 自动合并，可选开启 Dynamic Atlas
   - Laya：可启用 `AutoTextureConfig.enableAutoDynamicAtlas` 自动路径，或显式管理 `DynamicAtlasManager`；需按实际资源与发布配置验证

4. **数据支撑**：V4（12 散图 10000 精灵）实测：
   - Egret p50=17ms / Cocos p50=16.68ms / **Laya p50=33ms（慢 2 倍）**
   - 在本次未启用动态图集的交叉散图配置中，Laya dc=10000 与较高帧时同时出现；不能只凭该测试断言唯一因果。

---

## 8. 附：DrawCall 采集口径说明

本次测试三引擎 `drawCallAvg` 的采集方式及语义：

| 引擎 | 采集方式 | 语义 |
|---|---|---|
| Egret WebGL | `window.__webglProbe` hook `gl.drawElements` | 实际 GPU drawcall 次数（VBO flush 次数） |
| Cocos WebGL | `window.__ccGlProbe` hook `gl.drawElements` + multi_draw | 实际 GPU drawcall 次数（buffer 批次） |
| Laya WebGL | `window.__layaGlProbe` hook `gl.drawElements` | 实际 GPU drawcall 次数（纹理切换次数） |
| Egret WebGPU | `renderer._lastBatchCount` | 批处理器批次数 |
| Laya WebGPU | `CT_2DDrawCall`（引擎统计） | 合批后渲染元素数（≈ drawcall 数） |
| Cocos WebGPU | `window.__ccGlProbe`（探针兼容 WebGPU） | 实际 GPU drawcall 次数 |

**注意**：各引擎 drawCallAvg 在同一变体下数值不同，不可直接横向比较大小。意义在于：
- 同一引擎内不同变体的 dc 变化 → 反映该引擎的合批能力
- 配合帧时数据 → 判断 DrawCall 是否是帧时瓶颈
