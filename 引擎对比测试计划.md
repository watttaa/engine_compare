# H5 游戏引擎选型 · 渲染性能对比测试计划

> **对象**：自研 Egret 5.4.1 vs Cocos Creator 3.8.8 vs LayaAir 3.4.0
> **目标**：为企业级 2D/2.5D 回合制 MMORPG（对标《大话西游》H5，三端发布：Web / 微信小游戏 / 抖音小游戏）提供**可量化、可复现、可信**的引擎选型依据。
> **日期**：2026-08-26　**状态**：计划评审稿

---

## 一、为什么做这件事

引擎选型不能靠"官方宣传"或"感觉"。三个引擎各有性能声明，但：
- 各自的性能面板**口径不同、精度不同**，数字不能直接横向比较；
- 官方 demo 场景各不相同，无法回答"我们的游戏用哪个更快"。

因此我们搭建一套**统一测试框架**：同一套测试场景、同一套采集代码、同一台机器，跑三个引擎，得出可直接对比的数字。

---

## 二、对比什么（测试场景）

按"从标准基准到真实业务"分四类场景，每类考察不同的渲染压力：

| # | 场景 | 一句话特点 | 对应业务 | 参与引擎 |
|---|---|---|---|---|
| 1 | BunnyMark | 拆因子——定位瓶颈在哪 | 飘字/弹幕/UI 混排 | 三引擎 |
| 2 | 水族馆 2D | 组合压力——验证真实表现 | 场景生物/NPC 游走 | 三引擎 |
| 3 | 骨骼动画 | MMO 第一大开销 + 三引擎差异最大 | 角色/NPC/怪物 | 三引擎 |
| 4 | 水族馆 3D | 2.5D 路线能力评估 | 未来演进 | Cocos/Laya |

### 场景 1 · BunnyMark（2D 精灵压力，业界标准）

大量精灵移动弹跳，是渲染引擎的通用体检项。

**设计特点：控制变量实验**——4 个变体一次只压一个因子，数据能精确定位"每个引擎的瓶颈在哪一项"，而不是只给一个混合分数：

| 变体 | 内容 | 考察能力 | 对应游戏场景 |
|---|---|---|---|
| V1 | 单一贴图，只移动 | 合批上限 | 飘字、弹幕 |
| V2 | 8 张贴图轮换 | 图集多帧合批 | 怪物群、图集动画 |
| V3 | 单贴图 + 每帧旋转缩放 | 变换计算开销 | Buff 特效 |
| V4 | 12 张贴图相邻不同 | 破批提交压力 | UI 混排 |

> - 物理参数逐行移植自 PixiJS 官方 bunny-mark，保证与业界基准可比
> - 规模档位：1,000 / 5,000 / 10,000 / 20,000（1 万为业界标准档），另做阶梯加压测承载力（上限 20 万）

### 场景 2 · 水族馆 Boids（2D 综合运动）

**设计特点：组合压力**——5 种"鱼"按 Lissajous 轨道曲线游动，每帧同时发生"旋转朝向 + 5 纹理混合 + 连续运动"，是贴近真实场景的混合负载（引擎可能单因子强、组合弱，或相反）。运动模型移植自 Khronos webglsamples Aquarium（Google I/O 演示同款经典基准）。对应场景生物、NPC 游走。

> **场景 1 与场景 2 的分工**（为什么两个都要）：
> - **BunnyMark 拆开测**——控制变量实验，4 个变体一次只压一个因子（纹理数 × 变换类型），数据能定位瓶颈在哪；
> - **水族馆合起来测**——旋转、多纹理、连续曲线运动每帧同时发生，验证引擎在混合压力下的真实表现。
> - 只测前者无法回答"组合起来快不快"，只测后者数据是混的、说不清瓶颈。**一个定位、一个验证**。
>
> 规模档位：100 / 500 / 1,000 / 2,000，另做阶梯加压测承载力。与 BunnyMark 用同一套贴图，保证纯变换差异。

### 场景 3 · 骨骼动画（★ MMO 最核心场景）

**设计特点：选型最关键的数据**——大量 Spine 骨骼角色同屏播放动画。这是 MMO 的第一大 CPU 开销（每帧要算骨架矩阵、蒙皮顶点、上传动态顶点缓冲），也是三引擎实现差异最大的地方（蒙皮走 CPU / GPU / WASM 各不相同，合批策略也不同——同一骨架的多实例是否共享绘制批次，直接拉开数量级差距）。

**资源真实性**：用**大话西游项目真实角色资源**（狐美人、虎头人、逍遥生等 8 个角色 + Spine 官方标准角色 spineboy），配大话西游战斗场景背景，而非玩具样例。

| 变体 | 内容 | 考察 | 对应场景 |
|---|---|---|---|
| S1 | 同一角色 × N，同动画 | 同骨架合批 + 蒙皮成本 | 同类怪海 |
| S2 | 8 种角色混合 | 多骨架破批 | 玩家/NPC 混编同屏 |
| S3 | 同角色 × N，多动画混播 | 动画状态混合 | 玩家各异动作 |

> - 规模档位：20 / 50 / 100 / 200（蒙皮 + 顶点上传开销重，档位低于纯精灵场景；20 为 MMO 常态同屏角色量级，200 为帮战/世界 BOSS 量级）
> - 附加指标：蒙皮顶点总数/帧（负载归一化基数），并重点观察**同骨架多实例是否共享 drawCall**（引擎合批能力的分水岭）

### 场景 4 · 3D 水族馆（2.5D 能力，仅 Cocos/Laya）

**设计特点：演进路线评估**——用原版 Aquarium 的 5 鱼种 3D 模型 + 漫反射/法线贴图 + 固定三点光照，考察若产品未来走 2.5D 路线（2D 场景 + 3D 角色或全 3D 表现）时两引擎的 3D 渲染能力。Egret 5.x 无 3D，不参与此项。

> - 规模档位：50 / 100 / 200 / 400（3D 负载重，档位最低）
> - 专属指标：三角形数/帧（两引擎官方计数器都有，口径可比）——用于找 GPU bound 转折点
> - 与场景 2 同款运动模型，2D/3D 数据可对照

---

## 三、对比哪些指标（且都能准确采集）

> **准确性是本计划的生命线。** 每个指标都经过源码级验证，明确标注采集方式与可信度。

### 核心指标（三引擎口径完全统一，可直接横向对比）

| 指标 | 含义 | 采集方式 | 为什么准确 |
|---|---|---|---|
| **帧时间 P50/P95/P99** | 一帧耗时的中位数/95分位/99分位（毫秒） | 三引擎共用**同一份采集代码**，用浏览器高精度计时器 `performance.now()`（微秒级） | 不依赖任何引擎自带面板，零口径差异。P95/P99 能看出卡顿抖动，比平均 FPS 精确得多 |
| **drawCall/帧** | 每帧向 GPU 提交的绘制批次数 | 各引擎官方计数器，统一"采样期每帧平均" | 数字源自引擎内部计数，不可能造假。已逐一核对三引擎 API 并修正过 LayaAir 的一处读取错误 |
| **承载力 MaxStableCount** | 60fps 下能稳定渲染多少个对象 | 逐级加量到掉帧，回退一档 | 最直观的"能扛多少"，判定标准三引擎统一 |
| **掉帧率** | 超预算（>16.7ms）的帧占比 | 帧时间派生 | 与帧时间同源 |
| **JS 内存占用** | JS 堆内存峰值 | 浏览器 `performance.memory` | 同浏览器下可比（移动端闪退风险指标） |

### 场景专属指标

| 指标 | 适用场景 | 采集 | 可信度 |
|---|---|---|---|
| 三角形数/帧 | 3D 水族馆 | Cocos/Laya 官方计数 | 高（两引擎都有） |
| 蒙皮顶点数/帧 | 骨骼动画 | 适配器静态记录 × 角色数 | 高（可精确算出） |

### 平台指标（小游戏生死线，发布产物实测）

| 指标 | 标准 | 意义 |
|---|---|---|
| 首包体积 | 微信主包 ≤ 4MB | 超限过不了审 |
| 启动耗时 | 冷启动到首帧 | 微信考核项，影响留存 |
| WebGPU 降级 | 无 GPU 时能否优雅回退 WebGL | 小游戏兼容性 |

### 明确不采集的指标（诚实声明）

| 指标 | 为什么不采集 |
|---|---|
| GPU 帧时间 | 三引擎内置统计**都没有**，扩展 API 在移动端/小游戏不可用，强测不准 |
| setPassCall | 只有 Egret 有此统计，无法三方对比 |
| GPU 显存 | Egret 无此 API（仅 Cocos/Laya 有），不进核心对比 |
| 各引擎自带面板的帧时间 | 精度不足（Egret 用毫秒整数、Laya 用 1000÷fps 换算），且口径不一，只做人眼参考 |

---

## 四、后端对比（WebGL vs WebGPU）

三引擎都测两个渲染后端。**用"引擎内自比"控制变量**：同引擎同场景，只切后端，比帧时间——差值就是纯后端收益（如"WebGPU 是 WebGL 的 1.8 倍"）。

> **为什么现在就测 WebGPU（平台侧动态）**：2026-05 微信小游戏开发者大会官宣小游戏 WebGPU 进入**内测**（多线程 + WebGPU + 直玩小游戏提效，官方演示 WebGL 20-30 帧场景切 WebGPU 管线后 60 帧满帧）。LayaAir 3.3+ 已提供 WebGPU 配置、3.5 预告 WebGPU Driven 渲染架构。这意味着 WebGPU 不是浏览器端的前瞻性实验，而是**微信端可预期的近期红利**——现在测的 WebGPU 收益数据，直接对应小游戏端未来的性能上限。

| 引擎 | WebGL | WebGPU |
|---|---|---|
| Egret 自研 | ✅ | ✅ 自研 WebGPU 后端（已验证可跑） |
| Cocos 3.8 | ✅ | ✅ 引擎内置 |
| LayaAir 3.4 | ✅ | ✅ IDE 内置（发布后浏览器生效） |

> 遵循 WebGPU 规范协作者 toji.dev 的公平性方法论：强制两后端用同一 GPU、关闭抗锯齿对齐、固定分辨率——避免"以为在比 API、其实在比两块显卡"的常见陷阱。

---

## 五、如何保证公平（每轮测试前核对）

- 同一台机器、同一浏览器版本、插电、关省电、关无关插件
- 三引擎同一套测试代码、同一套贴图资源
- 渲染区域固定 1280×720，抗锯齿关闭，分辨率一致
- 先测 WebGL 后测 WebGPU，中途不改任何代码
- 每轮先预热 3 秒再采样 10 秒（隔离编译/加载的一次性开销）
- 结果自动记录环境指纹（GPU 型号、浏览器、分辨率）到 JSON

---

## 六、产出

每个"引擎×场景×后端×规模"组合产出一份 JSON，汇总成对比看板：

1. **性能对比表**：P95 帧时间（越小越好）
2. **承载力表**：能扛多少对象
3. **WebGPU 加速比表**：各引擎后端收益
4. **合批效率表**：drawCall 对比
5. **小游戏指标表**：包体 / 启动 / 内存

---

## 七、可行性评估（诚实交底）

### ✅ 已验证可行

| 项 | 验证方式 | 结果 |
|---|---|---|
| Egret WebGL/WebGPU bunnymark | headless 浏览器实跑 | 通过：帧时/drawCall/承载力全出数据，WebGPU 降级正常 |
| 三引擎 drawCall 采集 | 源码逐行核对 | 通过（修正了 LayaAir 一处 2D drawCall 读取错误） |
| **drawCall 双后端可用性** | 源码核对 | 通过：Egret WebGPU 走批处理器计数、Cocos `webgpu-device.ts:686` 每帧同步队列计数、Laya `laya.webgpu_2D.js` 的 `CT_2DDrawCall` 在 WebGPU 路径有记录——**WebGL/WebGPU 两后端 drawCall 均可采** |
| 帧时间统一采集 | 已跑出真实数据 | 通过（微秒精度，三引擎同代码） |
| 骨骼动画资源 | 已从项目复制 | 就位（11 套 Spine + 大话西游背景，三引擎 Spine 运行时齐全） |

### ⚠️ 尚需完成（风险与工作量）

| 项 | 现状 | 风险 | 工作量 |
|---|---|---|---|
| Cocos/Laya 工程实跑 | 代码+资源就位，**未实际编译运行过** | 中：需在各自 IDE 里跑通、验证数据 | 各 1-2 天 |
| 骨骼动画三引擎适配器 | 资源就位，**适配器未开发** | 中：三引擎 Spine API 不同 | 3-4 天 |
| 3D 水族馆适配器 | 资源就位，**未开发** | 中：3D 场景搭建 | 3-4 天 |
| GPU 一致性/环境记录 | **未实现**（公平性关键项） | 低：采集代码补充 | 0.5 天 |
| 小游戏发布测试 | 未开始 | 中：三端发布配置 | 3-5 天 |
| WebGPU 真机验证 | 仅验证过降级路径 | 低：需真 GPU 浏览器确认 | 0.5 天 |

### 关键诚实声明

1. **测试场景为本项目设计**，物理/运动模型移植自 PixiJS bunny-mark 与 Khronos Aquarium（业界公认基准），骨骼动画用大话西游真实资源。业界不存在"标准三引擎对比套件"，所有严肃跨引擎对比都是自建场景——这是通行做法。
2. **数据准确性有保障**：核心指标不依赖各引擎自带面板（精度不齐），而用统一代码采集，这比直接看三个面板更准。
3. **结论会标注适用边界**：性能只是选型的一个维度，还需结合工具链、生态、招聘、热更新等综合判断（另有选型报告覆盖）。

---

## 八、建议排期

| 阶段 | 内容 | 周期 |
|---|---|---|
| P0 | 补齐公平性采集（GPU/环境记录）+ Cocos/Laya 跑通 2D 基线 | 3 天 |
| P1 | 三引擎 2D 全场景（BunnyMark + 水族馆）WebGL/WebGPU 数据 | 3 天 |
| P2 | 骨骼动画三引擎适配 + 数据（MMO 核心场景） | 4 天 |
| P3 | 3D 水族馆（Cocos/Laya） | 3 天 |
| P4 | 小游戏三端发布 + 包体/启动/内存 | 5 天 |
| P5 | 汇总报告 | 2 天 |
| **合计** | | **约 3-4 周** |

> 若时间紧，P0+P1+P2（2 周）即可产出 MMO 选型的核心结论（2D 渲染 + 骨骼动画 + WebGPU 收益）。
