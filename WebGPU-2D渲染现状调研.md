# WebGPU 对 2D 渲染的真实价值 · 三引擎调研

> **背景**：我们的三引擎基准（BunnyMark V1-V4）测出一个关键发现：WebGPU 在 2D 场景的价值
> **完全取决于引擎是否实现了多纹理合批**。本文汇总实测数据 + 三家官方文档/社区证据，
> 供选型报告引用。
> **日期**：2026-08-27 · 数据来源：本项目统一基准（sim-core 同物理同采集）+ 官方文档/论坛

---

## 一、核心实测发现（BunnyMark · 10000 只 · 真机 WebGPU）

| 变体 | 场景含义 | Egret WebGPU | Laya WebGPU |
|---|---|---|---|
| V1 单贴图 | 合批已饱和（飘字/怪海） | dc=1，与自家 WebGL 打平（58fps） | dc=2，与自家 WebGL 打平（57fps） |
| V2 8 纹理 | 图集帧混排 | **dc=1，58fps**（WebGL 9999dc/41fps） | **dc=10000，30fps**（与自家 WebGL 一样烂） |
| V3 单贴图+变换 | Buff 特效 | dc=1，58fps | dc=2，**比自家 WebGL 慢 24%**（34 vs 45fps） |
| V4 12 纹理 | UI 混排 | **dc=1，58fps**（WebGL 9999dc/42fps） | **dc=10000，24fps**（比自家 WebGL 还慢） |

**一句话结论**：多纹理合批是 2D 上**最直接可见**的 WebGPU 收益（谁实现了谁的破批场景
就有 40%+ 帧时间收益），但不是唯一收益面——完整收益谱见第五节，其余多数同样依赖引擎
投入，少数是架构层"免费"收益。

---

## 二、Laya：官方把 WebGPU 收益明确圈定在 3D

| 证据 | 内容 | 结论 |
|---|---|---|
| 官方文档（WebGPU 配置页） | "可显著提升 Web 应用在 **3D 渲染、复杂后处理以及 GPU 计算任务**下的运行性能"——只字未提 2D | 2D 不在优化范围 |
| 官方 3.4 性能对比视频 | 宣传点全部是 3D：Forward+、Cluster Lighting、GPU Instance、1473 万三角面 | 同上 |
| 引擎源码（3.4 webgpu_2D.js） | 顶点管线有 `textureArrayIndex`（纹理阵列层号）**读取**代码，但全引擎无一处赋值——**多纹理合批的架子搭了，逻辑没接** | 源码级实锤 |
| 社区论坛（ask.layabox.com/t/webgpu） | 只有 Spine 颜色异常、2D 灯光裁切等 bug 反馈，无 2D 合批讨论 | 公开领域空白，我们的一手数据有价值 |
| 微信端实测（公开报道） | 小米15 约 1.5 倍提升，"内容越复杂提升越明显"——重内容/3D 场景 | 与我们数据不矛盾（2D 轻内容是盲区） |

**对 Laya 的公平表述**：不是"Laya WebGPU 差"，而是 **Laya 的 WebGPU 投入在 3D；截至
3.4，2D 场景无收益甚至有回退**（变换开销更高）。其 3.5 路线图（WebGPU Driven 架构）
可能补齐——届时用同一套基准复测即可。

---

## 三、Cocos：WebGPU 实验性 + 2D 合批仍是 WebGL 思路

| 证据 | 内容 | 结论 |
|---|---|---|
| 官方发布文档 | 自 3.6.2 支持，标记 **Experimental（实验性）**，仅 **Web-Desktop** 构建可选，需 Chromium 105+ | 成熟度最低（vs Laya 已进微信小游戏） |
| 官方合批规则文档 | 合批条件：同材质/同纹理源/同 blendState——**纹理切换即断批**，未提及纹理阵列/多纹理打包 | 2D 合批仍是"同纹理才合"的 WebGL 思路 |
| 引擎源码（3.8.8 本地） | 2D 合批走 `WEBGL_multi_draw` 扩展（一次提交多次绘制），非纹理阵列合批 | multi_draw 减少提交次数 ≠ 消除断批 |
| 官方论坛 | 3.8.4 提及 WebGPU 性能提升与 RenderGraph——叙事偏 3D 管线 | 与 Laya 同样的 3D 倾向 |
| **我们的实测补充** | 3.8.8 WebGL 后端 `device.numDrawCalls` **引擎级恒 0**（命令缓冲从不递增）——官方性能面板在 WebGL 构建里也不可信 | 选型报告可引用的引擎质量数据点 |

**待补**：Cocos WebGPU 臂的真机实测（网站上已就绪，跑全轮次即出数据）——重点看 V2/V4
的 dc 是 1 还是 10000，直接判定它有没有做 2D 多纹理合批。

---

## 四、Egret 自研：合批是谱系化的持续投入（需披露利益相关）

自研引擎的合批能力是**四条渐进路线**（源自大话西游项目实查，`h5client_s2`）：

| 路线 | 机制 | 状态 |
|---|---|---|
| ① 原生同纹理合批（WebGL） | egret 批次器按纹理对象身份合并，同纹理精灵一批 | 引擎原生，一直在用 |
| ② **动态合图系统（WebGL，自研）** | `TextureAtlasManager`：2048×2048 自动大图集（最多 10 张），≤512×512 纹理自动放入共享图集 → 跨图片合批；`BatchType` 逐纹理控制（业务给模型打 `IgnoreSelf`、Spine 打 `Disable` 排除 UV 改写类内容） | **引擎完整实现；生产默认关闭**（`openAutoBatch=false`）。**基准实测**：裸环境开开关会白屏——TextureAtlasManager 需游戏运行时的宿主上传回调初始化（`onCreateAtlas`/`onUploadCell`），无法靠开关单独生效。这解释了生产为何默认关；基准按生产真实状态=关闭 |
| ③ **SDF 字形合批（WebGL，自研）** | 引擎层 `sdf_text` program：atlas 打 `isSDF` 标志 → 逐字形走标准批次器；同图集+同样式合批，颜色走顶点 tint 不断批（Godot 同构）；在线补字+动态 atlas 增量上传 | **已上线**（2026-08-25 定稿） |
| ④ **多纹理合批（WebGPU，自研）** | webgpu 批处理器纹理槽：**不同纹理**直接合批，V2/V4 dc 9999→1 | 已实装（本基准验证） |

**四种路线的本质差异**：
- ①③ 是"同纹理才合"；②把不同图片**搬进同一张纹理**（资源组织型，有尺寸≤512、UV 改写冲突等约束）
- ④在渲染层直接跨纹理合（渲染技术型，无合图约束、无内容排除）
- ②与④解决同一问题（跨图片合批），②受 UV 改写/图集容量约束并在本项目停用，④无此约束——
  **这正是"WebGPU 时代才做得干净"的那类优化**

汇报时披露：Egret 为自研引擎，②③④为本团队开发——数据真实，但评审应知悉关系。
对领导的一句总结：**"跨图片合批我们两代方案都做过——WebGL 时代用动态合图（因 UV 冲突
等约束未全面启用），WebGPU 纹理槽方案彻底解决；加上已上线的 SDF 文字合批，合批是我们
渲染管线上持续的、有积累的投入方向。"**

---

## 五、WebGPU 对 2D 的完整收益面（不止合批）

调研结论：合批是最容易被基准测出的一项，但 WebGPU 给 2D 的还有这些：

### 5.1 需要"引擎主动实现"的收益（和合批同一性质）

| 能力 | 2D 游戏用途 | 现状 |
|---|---|---|
| **Compute Shader 粒子** | Buff/技能特效——粒子物理全 GPU 计算，免 CPU↔GPU 传输（社区实测 2D Boids 6700 万粒子 @30fps） | 三家引擎均未提供 2D 粒子 compute 方案；Laya 的 compute 叙事在 3D。**自研可做** |
| **SDF/MSDF 文本渲染** | 聊天框/角色名/伤害飘字——任意缩放无锯齿，字形渲染走 shader | 需引擎集成（官方 sample 有成熟方案）；三家 2D 均未内置 |
| **Render Bundles（命令重放）** | 静态 UI 面板——预录制绘制命令，重放 CPU 成本极低。**Figma 用它渲染复杂 UI** | 引擎需为静态子树做录制/失效管理；未见三家实现 |
| **视频外接纹理** | 过场动画/直播纹理——`importExternalTexture` 零拷贝上屏 | 引擎级集成即可用，价值看产品形态 |

### 5.2 架构层"半免费"的收益（无需引擎特别优化）

| 能力 | 说明 |
|---|---|
| **管线状态预验证（PSO）** | WebGL 在每次 draw 时验证状态；WebGPU 在创建管线时一次验证——draw 越多、状态切换越频繁，收益越大 |
| **Bind Group 资源绑定** | 替代 WebGL 全局状态机，资源绑定结构化、可预测（也是合批的基础设施） |
| **异步错误处理** | 出错不卡主线程不炸渲染，线上稳定性 |
| **原生压缩纹理** | BC（桌面）/ASTC/ETC2（移动）原生支持，内存带宽友好 |

### 5.3 对我们实测数据的解释力

- **V1 打平**（Egret/Laya 的 webgpu ≈ webgl）：5 次 draw 的场景验证开销本来就低，
  PSO 预验证的收益显示不出来——"免费收益"在轻负载下接近零
- **Laya V3 反而慢**：架构收益被它的实现开销（每帧矩阵处理/上传路径）吃掉了——
  印证"半免费也要引擎不添乱"
- **Egret V2/V4 大胜**：合批（引擎投入项）兑现后，收益立即显性化

### 5.4 对自研路线的意义

5.1 的四项能力（compute 粒子、SDF 文本、Render Bundles、视频纹理）都是**引擎层可自主
规划的增强**——自研 Egret 的 WebGPU 后端已经把合批做了，后续可按产品需要逐项加：
**特效密集的战斗场景 → compute 粒子优先；文本密集的 UI → MSDF 优先**。这是选型自研
引擎的长期红利：收益路线图握在自己手里，而 Laya/Cocos 的 2D WebGPU 排期不由我们决定。

---

## 六、对选型的意义（2D MMO 视角）

1. **"上 WebGPU"不是性能开关，是引擎工程投入方向**。三家里只有自研 Egret 在 2D WebGPU
   上做了合批优化——对 2D MMO（UI 混排、多皮肤角色、图集帧动画）这是直接收益
2. **图集是兜底方案**：V2/V4 测的是"最坏情况纹理交叉"。真实项目把资源打进同一图集可
   让 WebGL 也恢复合批——但受美术管线约束（角色皮肤/特效天然跨图集），不能全靠它
3. **微信小游戏 WebGPU 已内测**（2026-05 大会官宣）：存在，但吃到红利的前提
   是引擎把 2D 路径优化好——这正是三家分化的地方
4. 微信端 WebGPU 对小游戏的实际收益，待平台放量后用同一套基准在小游戏容器里复测

---

## 附：数据可信度说明

- 帧时间：`performance.now()` 微秒级，同一份采集代码（sim-core），预热 3s + 采样 10s
- drawCall：各引擎内部计数器/API hook，口径差异已标注（Egret/Cocos=GPU 提交数，
  Laya=合批后渲染元素数）
- 环境：同一台真机、同分辨率、抗锯齿全关、Chrome 最新版
- 局限：单档数量（10000）、未测 Cocos WebGPU 臂（进行中）、未覆盖骨骼/3D 场景
