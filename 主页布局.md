# 主页布局说明（bench/web/tpl/home.html）

> 本文档描述站点主界面（`dist/index.html`）当前布局，作为改版需求底稿。
> **改需求方式：直接在本文末尾「二、需求区」写需求，我照着改。**
> 生成链路：`bench/web/build.js` 读 `bench/web/tpl/home.html` 模板 → 注入占位符 → 输出 `dist/index.html`。

---

## 一、当前布局（自上而下）

### 0. 全局

| 项 | 现状 |
|---|---|
| 视觉风格 | 仿 VitePress 默认主题（SDF-Kit 主站同款） |
| 主题 | 双主题：跟随系统明暗（`prefers-color-scheme`），无手动切换按钮 |
| 主色 | 品牌蓝 `#3451b2`（暗色 `#5672cd`），hero 标题用 蓝→绿 渐变字 |
| 字体 | 系统栈（PingFang SC / Microsoft YaHei） |
| 容器 | 居中，最大宽 1152px；移动端（≤640px）单列堆叠 |
| 顶栏 | 吸顶 + 毛玻璃半链接 |

### 1. Hero 首屏（居中三段 + 4 按钮）

| 元素 | 内容 |
|---|---|
| 大标题（渐变字） | 引擎对比基准 |
| 副标题 | 三引擎，同场、同题、同尺子 |
| 定位语 | 自研 Egret 5.4.1 × Cocos Creator 3.8.8 × LayaAir 3.4.0 —— 同一仿真核心逐字节共用、WebGL / WebGPU 双渲染后端、源码级采集口径、全部数字可复现 |
| 按钮 1（实心蓝） | 🏁 进入性能实测 → `scene-bunny.html` |
| 按钮 2（实心蓝） | 🐠 3D 鱼群承载力 → `scene-boids3d.html` |
| 按钮 3（浅底描边） | 📊 实测数据看板 → `dashboard/index.html` |
| 按钮 4（浅底描边） | 📋 选型结论速览 → 锚点 `#conclusions` |

### 2. 特性卡（2×2 图标卡，上边框分隔线）

| 图标 | 标题 | 一句话 |
|---|---|---|
| ⚖️ | 同一把尺子 | sim-core 四件三引擎逐字节同一份；`performance.now()` 唯一时间源；p50/p95/p99 + 掉帧档重试 |
| 🎛️ | 双渲染后端 | 每场景 WebGL/WebGPU 双臂对照；Cocos 单二进制切 renderMode；后端判定以画布实际 context 为准 |
| 🗺️ | 场景矩阵 | BunnyMark V1-V4 → 2D 鱼群 → 3D 水族馆承载力阶梯 → 预烘焙骨骼 MMO 切片 |
| 🔬 | 口径可审计 | drawCall 源码级核对（hook + 引擎计数器 + multi_draw 拦截）；假渲染防护、预热收敛、污染闸门 |

### 3. 「这是什么」（纯文字两段）

- 站点定位：把三家引擎放进同一个受控竞技场，先对齐口径再看差距
- 强调：性能实测是主环节之一，**结论、方法论、踩坑记录同为一等公民**，JSON 可导入看板核对

### 4. 「30 秒看效果」（4 条快速链接列表）

1. 直接跑 → 性能实测（默认 BunnyMark）
2. 看承载力 → 3D 水族馆阶梯压测
3. 看横向数据 → 数据看板
4. 看结论 → 选型结论速览

### 5. 「测试场景」（场景卡网格，2 列，构建期注入 `{{SCENE_CARDS}}`）

| 卡片 | 链接 | 副标题 |
|---|---|---|
| 🐰 BunnyMark | `scene-bunny.html` | 2D 精灵压力 · V1-V4 合批/破批/变换 |
| 🐟 水族馆 | `scene-boids.html` | 大量 2D 单位综合运动（旋转+多纹理） |
| 🐠 3D 水族馆 | `scene-boids3d.html` | Laya/Cocos 3D 鱼群 · GPU instancing · 网格/光照/尾巴动画 |
| ⚔️ 大话西游骨骼（预烘焙） | `scene-dhxy.html` | 8 职业混编 · 三层帧动画 + A* 随机行走（MMO 核心） |
| 🧵 布料 | `scene-cloth.html` | 布料/物理模拟（规划中） |

> 场景清单由 `build.js` 的 `SCENES` 数组驱动，加场景 = 改数组，主页自动同步。

### 6. 「选型结论速览」（锚点 `#conclusions`）

- 2D 表格：BunnyMark · 10000 精灵 · 真机帧率
  - V1 单纹理：Cocos 57.7/58.1 vs Laya 58.2/58.2（打平）
  - V2 8 纹理交叉：**Cocos 58.3/58.3** vs Laya 29.4/33.1（行标绿=优）
  - V3 单纹理+变换：**Cocos 58.6/57.3** vs Laya 43.3/41.4
  - V4 12 纹理交叉：**Cocos 58.2/58.1** vs Laya 38.0/29.8
- 3 条结论：① 2D 纹理交叉/变换密集 Laya 约为 Cocos 一半（三条根因）② 3D Laya 的 WebGPU 上限是差异化优势，Cocos GFX 双后端同源 ③ 自研 Egret WebGPU 承载力约为 WebGL 4-6 倍、dc 约 1/3000
- 脚注：数据出处（汇报稿 2026-08-31 / 自研总结 md）+ **画布物理像素口径警告**（Egret/Laya 固定 1280×720，Cocos 随窗口×DPR）

### 7. 「方法论要点」（锚点 `#methodology`，6 条列表）

sim-core 共用 / 唯一时间源 / 动态预热收敛 / p50/p95/p99 + 阶梯判定 / drawCall 双路径口径 / 公平性清单

### 8. 「现状一览」（锚点 `#status`，构建期注入 `{{ARM_STATUS}}`）

等宽字体逐行显示各引擎臂就位状态（✓ 绿 / ✗ 红），当前 8 行：

1. ✓ Egret 自研 5.4.1（WebGL + WebGPU）— build.js 全自动组装
2. ✓ Egret WebGPU vs WebGL 自比面板
3. ✓ LayaAir 3.4 WebGPU 臂 — vendor/laya
4. ✓ LayaAir 3.4 WebGL 臂 — vendor/laya-webgl
5. ✓ Cocos Creator 3.8.8 双后端臂 — vendor/cocos 单产物派生
6. ✓ LayaAir 3D（WebGL / WebGPU）— laya-proj-3d/release/web
7. ✓ Cocos Creator 3D（WebGL / WebGPU）— vendor/cocos3d
8. ✗ 骨骼 / 预烘焙多引擎臂（Spine · MC）— Egret 侧内置，Laya/Cocos 待 vendor 就位

+ 脚注：WebGPU 需 PC Chrome/Edge；缺产物放 vendor/ 自动上线。
+ 本页由 build.js 于 `{{BUILD_TS}}` 生成（显示在区块开头）。

### 9. 页脚

- 版权行：数据与方法文档见仓库 `openspec/` 与根目录报告 md · GitHub 仓库外链

---

## 二、需求区（在此写需求，我照着改）

> 格式建议：`[区块名] 需求描述`，如「[hero] 按钮改成三个」「[结论] 加 3D 数据表」
> 也可以写全局需求，如「整体改成浅色优先」「加返回顶部按钮」

<!-- 在下面写需求 -->

