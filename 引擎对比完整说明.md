# 引擎对比基准 — 完整说明(代码导读 × 对比方法 × 指标测量 × 可信度评估)

> 对比对象:**Egret 自研 5.4.1(WebGL/WebGPU)** vs **Cocos Creator 3.8.8(WebGL/WebGPU)** vs **LayaAir 3.4.0(WebGL/LayaX-WebGPU)**
> 测试环境:Windows + Chrome(单一时间源 `performance.now()`,同机同 GPU 同分辨率)
> 本文档是全仓库的导读与方法论总纲。逐场景执行标准见 `bench/BENCHMARK-SPEC.md`,快速上手见 `bench/README.md`。

---

## 一、这个仓库在做什么

回答一个工程问题:**同一套渲染负载,在 Egret / Cocos / LayaAir 三套引擎(× WebGL / WebGPU 两种后端)下,性能差距有多大、差在哪一层?**

为此仓库构建了一个"受控变量"基准体系:

1. **仿真与渲染解耦**——所有引擎跑同一份引擎无关的仿真代码(sim-core),CPU 侧计算量逐字节一致;
2. **参数逐项对齐**——分辨率 1280×720、DPR、60fps 上限、关抗锯齿、同一套贴图、同一预热/采样协议,全部写死在 `bench/BENCHMARK-SPEC.md`(唯一执行标准);
3. **结果可验证**——每个指标都有独立的探针/统计口径交叉印证,结论经历过多次反转与自我纠错(见第六节)。

---

## 二、目录与重要代码地图

```
engine_compare/
├── bench/                        ★ 基准框架本体
│   ├── BENCHMARK-SPEC.md           唯一执行标准(场景规格/参数/协议,任何偏差需在此登记)
│   ├── README.md                   快速上手 + 三引擎统计 API 出处
│   ├── sim-core/                   ★ 引擎无关层(三引擎逐字节同一份)
│   │   ├── stats.js                  帧时间采集与百分位统计
│   │   ├── bench-runner.js           fixedRun 固定采样 / rampRun 阶梯 / autoRamp 承载力
│   │   ├── bunny-sim.js              BunnyMark 物理(逐参数移植 pixijs/bunny-mark)
│   │   └── boids-sim.js / boids3d-sim.js  鱼群 2D/3D(移植 webglsamples aquarium)
│   ├── engines/                    各引擎适配器
│   │   ├── egret/                    Egret 自研引擎:main.js(适配器)+ 重编引擎库
│   │   ├── mc-bench/                 骨骼动画基准(Cocos/Laya/Egret 三版 Spine 适配)
│   │   ├── spine-bench/              Spine 骨骼仿真逻辑
│   │   └── egret-webgpu/             Egret WebGPU 后端注入面板
│   ├── assets/                     统一贴图(bunny-mark 原版 12 张 rabbitv3*.png)
│   ├── opt-www/                    优化专题页
│   └── web/                        ★ 静态站点组装器
│       ├── build.js                  ★★ 核心:从 vendor/ 构建产物派生全部"引擎×后端"双臂
│       ├── vendor/                   各引擎 IDE/Creator 构建产物(提交进仓库)
│       ├── tpl/                      总控面板 ctrl.js(自动测试/对比表)+ 页面模板
│       └── dist/                     组装输出(gitignore,CI 重建)
├── proj-cocos/                     Cocos 2D 基准工程(CocosBench.ts)
├── proj-cocos-3d/                  Cocos 3D 水族馆工程(FishBench3D.ts)
├── proj-laya/                      Laya 2D 基准工程(LayaBench.ts)
├── laya-proj-3d/                   Laya 3D 水族馆工程(Main.ts)
├── proj_cocos_skel/ proj-laya-skel/  骨骼动画基准工程(新建)
├── model/ lib/ scene/              骨骼基准依赖的游戏代码(mmc-bench 的动作库/寻路/场景)
├── benchmark 相关文档(md)          技术方案/管线对比/调研/汇报稿(根目录)
└── .github/                        CI(workflow:组装 dist 并部署 Pages)
```

**不在仓库(本地参考,已 gitignore):**`cocos-engine/`、`layaair/`(两份引擎源码,用于逐行核对统计 API 与渲染管线)、`webglsamples.github.io/`(Khronos 原版水族馆)、`egret-selfdev/`(仅 5.4.1/build/egret 引擎库入库)、`engine_minigame/`(微信小游戏对照工程)。

---

## 三、对比架构:四层受控设计

### 3.1 第一层:统一仿真核(sim-core)

所有引擎加载**同一份** `bunny-sim.js` / `boids-sim.js` / `boids3d-sim.js`,鱼/兔的运动完全由这份代码算出,引擎只负责"把它画出来"。这消除了最大的不公平源——各引擎示例自带的仿真逻辑差异。仿真按帧推进(不用引擎 dt),三引擎一致。

### 3.2 第二层:适配器(adapter)

每个引擎一个薄适配器,实现统一契约:`init / setCount / step / readDrawCalls / nodeCount / readBenchMetrics`。适配器只做"引擎 API 翻译",不含任何基准逻辑。

### 3.3 第三层:双臂派生(web/build.js)

同一份 Creator/IDE 构建产物(`vendor/`),由 `build.js` 派生出演示"仅后端不同"的两臂。例如 Cocos:同一份产物,只改 `renderMode: 4(WebGPU)/ 2(WebGL)` 派生 `dist/cocos` 与 `dist/cocos-webgl`。附带热补丁:注入 drawCall 探针、关抗锯齿、同步最新 sim-core、修正引擎 bug(如 Laya 遮挡剔除误杀、Cocos 实例化宏缺失——每次补丁都在对应文档登记)。

### 3.4 第四层:总控自动化(web/tpl/ctrl.js)

编排页用 iframe 逐臂执行 `?auto=1` 参数驱动的自动测试,回读 `window.__benchLastResult` 存 localStorage,渲染出横向对比表(固定点 p95 表 + 承载力 cap 表 + count→p95 曲线),支持一键全矩阵。

---

## 四、核心指标怎么测出来

### 4.1 帧时间与百分位(sim-core/stats.js)

- **唯一时间源** `performance.now()`,逐帧记录帧间隔(毫秒);
- 协议:**预热 3 秒**(丢弃,吸收 JIT/着色器编译/缓存冷启动)→ **采样 10 秒**(约 600 帧样本);
- 输出 `p50 / p95 / p99 / p1Low(最差1%均值) / stdDev / fps(均值帧率)`;
- **为什么不只看平均 FPS**:平均 FPS 会被"长期卡死+长期空闲"平均掉;p95 才代表真实体验的卡顿上限。判定用 p95 是行业通行口径。

### 4.2 Draw Call(三引擎三种口径,均已源码核实)

| 引擎 | 口径 | 说明 |
|---|---|---|
| Egret | `egret.sys.getPerformace().drawCall` | 引擎官方统计,1 秒窗口均值 |
| Cocos | WebGPU:`device.numDrawCalls`;WebGL:**GL 探针** | 3.8.8 WebGL 后端 numDrawCalls 恒 0(webgl2-command-buffer 不递增)→ build.js 注入探针 hook `drawElements/drawArrays/*Instanced/multi_draw`(含 getExtension 拦截扩展对象方法) |
| Laya | WebGPU:**statAgent 原生统计**(`CT_DrawCall`/`CT_3DDrawCall`/`CT_Triangle`);WebGL:GL 探针 | LayaX 渲染提交在原生层,JS 侧 GPURenderPassEncoder 钩子**测不到**——这是本次基准的重要方法论发现:原生层引擎必须用引擎原生统计,JS 探针只能测 Web 类引擎 |

所有 dc 探针为**窗口均值**(drawTotal/frameTotal),口径跨引擎一致。

### 4.3 承载力(autoRamp,3D 水族馆/2D 水族馆主指标)

"稳定门槛"定义:**fps ≥ 55 且 p95 ≤ 20ms**。流程:

1. 预设档位阶梯(如 3D:[50 → 100000] 共 20 档;2D:[2000 → 80000]);
2. 每档:预热 5s + 采样 6s → 若疑似掉帧,**加倍预热重测一次**(排除 GC/着色器编译尖峰的假阳性);
3. 连续 2 档未稳定才进 **+500 细扫**(cap 粒度从粗档位细化到 ±500);
4. **脏曲线防护**:低档掉帧后高档又恢复 → `invalidCurve=true`,结果作废重跑(防止调度/降频污染);
5. 输出:`cap(最高稳定档) / jankAt(掉帧档) / levels[](逐档 fps/p50/p95/p99/dc)`。

### 4.4 后端真实性校验(防"假 WebGPU")

WebGPU 不可用时引擎会**静默回退 WebGL**——这是对比测试的大坑。三层防护:

- 逐档记录 `actualBackend`(运行时真实后端,如 Cocos 以 `device.gl` 存在与否判定);
- 结果汇总时聚合各档 `actualBackend`:若同一臂内出现混合后端(中途回退)→ `backendValid=false` 标红;
- 对比表直接显示"实际后端"列,请求与不符的行标记无效。

### 4.5 骨骼动画基准(mc-bench / spine-bench)

预烘焙骨骼动画(8 职业混编、三层帧动画 + A* 随机行走),考察"多角色同屏"这一 MMO 核心负载;三引擎共用同一份寻路/仿真逻辑(`bench/engines/mc-bench`),指标口径同上。

---

## 五、公平性控制清单(逐条落地,非口号)

| 控制项 | 实现方式 |
|---|---|
| 仿真 CPU 成本一致 | sim-core 逐字节同一份;按帧推进不用引擎 dt |
| 分辨率/视口 | 固定 1280×720(禁各引擎自适应) |
| DPR | 统一按 devicePixelRatio(egret 显式 `calculateCanvasScaleFactor`) |
| 抗锯齿 | 统一关(WebGL 上下文 `antialias:false`;对齐 WebGPU 单采样;Egret/Cocos/Laya 均已落实) |
| 贴图/模型 | 同一套文件(12 张 rabbitv3 / 5 鱼种模型+贴图) |
| 帧率上限 | 统一 60 |
| 判定协议 | 同一预热/采样窗口、同一稳定门槛、同一 autoRamp 流程(sim-core 层实现) |
| 环境 | 同机、同浏览器版本、插电、记录 GPU/UA 于结果 JSON |
| 后端真实性 | 逐档 actualBackend 校验(见 4.4) |
| 视锥剔除口径 | 3D 侧显式对齐(Cocos `frustumCulling=false` / Laya `frustumCulled=false`,双引擎全量提交) |

已知的**恒定成本不对称**(逐项核查过,方向均对 Cocos 不利,因此差距是保守下界):Laya 侧多一份环境绘制基线、天空盒;这些不随鱼数缩放,不影响"承载力差多少倍"的结论方向。

---

## 六、可信度评估(诚实版)

### 6.1 可信的部分,以及为什么可信

1. **同代码同资源同判定**:三引擎跑的是同一份仿真与统计代码,对比的是"引擎渲染层"这一个变量——这是本基准最大的方法论优势;
2. **指标可交叉验证**:承载力结论有独立的反证支撑(例:Cocos 1.3 万鱼 dc=120 掉帧 vs Laya 10 万鱼 dc=369 满帧 → 排除 draw call 数因素,定位 CPU 提交层;LayaX 原生 statAgent 的 `CT_Triangle` 随鱼数线性增长 + 渲染节点计数 `2×N+195` 精确吻合 → 证明全量渲染,排除"偷画");
3. **探针双口径互证**:同一 dc 用引擎官方统计与 GL 探针两种方式测过,数值吻合;
4. **过程自我纠错**:结论经历过三次反转(LayaX 白模→查明是构建派生漏拷 shader;"只画 1.7% 鱼"→查明是 JS 探针测不到原生提交;遮挡误杀→查明默认开启的遮挡剔除),每次反转都把测量方法往更严谨推了一步——最终结论是被反复攻击后幸存的;
5. **可复现**:全部代码/参数/协议在仓库内,构建→预览→一键自动测试,任何人可跑出同表。

### 6.2 已知局限(使用结论时必须带的边界)

1. **测的是"默认/惯用路径",不是引擎天花板**:Cocos 侧未做深度优化(如绕开逐 Node 场景图的自定义提交)。Laya 侧为达到正确渲染打过库级补丁(实例化宏、遮挡剔除等)——存在"补丁精力不对称"。因此结论应读作"**开箱路径下 LayaX 承载力 >10×**,而非引擎能力上限";
2. **场景偏向性**:水族馆(boids)是"海量独立小对象+逐对象动画"负载,天然惩罚 O(N) JS 场景图遍历、奖励 GPU-driven 管线——这是**定向压测**,不是综合性能排名;
3. **WebGPU 对比含架构代差**:Laya WebGPU 走 LayaX 原生管线(GPU-driven/indirect draw),Cocos WebGPU 仍是 JS 提交——同族对照(WebGL vs WebGL)数据已补测,解读时两条线要分开;
4. **未覆盖项**:真机(移动端)未测;分钟级内存/GC 漂移未测;LayaX 3.4 为实验管线,行为可能随版本变化;
5. **单机单环境**:所有数据来自同一台 Windows + Chrome(已记录 GPU/UA)。跨设备排序不保证迁移。

### 6.3 一句话结论

> 在"海量独立动画对象"这一 MMO 核心负载、各引擎默认路径下:**LayaX(WebGPU)承载力为 Cocos(WebGPU)的 10 倍以上;差距不是 GPU、不是 draw call 数,而是每帧 O(N) 的 CPU 提交架构**(Cocos JS 单线程逐节点组织渲染 vs LayaX 原生层+GPU-driven 与对象数解耦)。该结论在本基准边界内证据链完整、可复现;超出边界(如"任何场景都快 10 倍""Cocos 做不了大场景")的引用都是误读。

---

## 七、复现指南

```powershell
# 1. 组装静态站点(从 vendor/ 派生全部双臂,含探针/热补丁注入)
node bench/web/build.js

# 2. 本地起服务
cd bench/web && npx http-server dist -p 8080
# 打开 http://localhost:8080/index.html(导航页,按场景分组)

# 3. 自动测试:进入任一场景页(如 scene-boids3d.html)
#    → 点「🔄 全轮次自动测试」:逐引擎×逐后端×逐变体跑 autoRamp/fixed
#    → 完成后自动渲染对比表(承载力 cap + count→p95 曲线)

# 4. 数据:localStorage 持久化;单臂结果同时下载 JSON(bench_<...>.json)
#    可拖入 bench/dashboard/index.html 做透视对比
```

关键参数入口:档位阶梯/稳定门槛在 `bench/sim-core/bench-runner.js`;场景规格在 `bench/BENCHMARK-SPEC.md`;各臂派生补丁在 `bench/web/build.js`。

---

## 八、变更纪律

1. 任何场景参数改动必须先改 `bench/BENCHMARK-SPEC.md` 并 bump 版本号,再改代码;
2. 任何 build.js 热补丁必须在补丁处注释登记"哪个引擎 bug、现象、修复方式";
3. 结论变更(尤其是反转)必须在新文档记录"旧结论/反证/新结论"三段,不许静默改数。
