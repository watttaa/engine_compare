# 三引擎 2D 渲染管线 · 源码级精确对比

> 每个结论都标注源码位置，可直接进选型技术报告。基线：Cocos 3.8.8 / LayaAir 3.4 / Egret 自研 5.4.1

---

## 一、dc（draw call）差异的精确根因

### 1. Egret WebGL —— 同纹理合批，纹理一变即断

**源码**：`egret.web.js` → `WebGLDrawCmdManager.pushDrawTexture`

```js
if (hierarchy || this.drawDataLen == 0 ||
    this.drawData[this.drawDataLen - 1].type != 0 /*TEXTURE*/ ||
    texture != this.drawData[this.drawDataLen - 1].texture ||  // ← 纹理身份不同 → 新 draw call
    this.drawData[this.drawDataLen - 1].filter) {
    // 新建 drawData 条目 = 一次新提交
}
```

**结论**：纹理对象身份一变就断批。V2(8纹理)/V4(12纹理) 轮换分配 → 相邻必不同 → dc ≈ 精灵数（9999）。这是经典"同纹理合批"，WebGL 时代的通解。

### 2. Laya（WebGL + WebGPU 相同）—— 普通 Sprite 无合批

**源码**：`laya.webgpu_2D.js` → `drawRenderElementList`

```js
for (let i = 0, n = list.length; i < n; i++) {
    elements[i]._render(this, this.renderCommand);   // 每精灵一次提交
}
this._submit();
Laya.LayaGL.statAgent.recordCTData(StatElement.CT_2DDrawCall, list.length);
//                                                      ^^^^^^^^^^^ = 精灵数
```

**结论**：`list.length` 就是精灵数。普通 Sprite 不进任何批处理器（`BatchManager.registry` 只注册了 `sequenceFrame2D`，Sprite 走 `NullBatchProvider` 逐个透传）。所以 dc 恒等于精灵数，且**两后端一致**。

### 3. Cocos —— multi_draw 扩展一次提交多图元

**源码**：`cocos/gfx/webgl2/webgl2-commands.ts`

```js
// WEBGL_multi_draw 扩展可用时：
md.multiDrawElementsWEBGL(glPrimitive, counts[], offsets[], 0, ..., drawCount);
// 一次调用提交 N 组 (count, offset) —— 一个 draw call 承载多个不连续图元
```

**结论**：多纹理不同也**不断批**（顶点缓冲按批次组织，multi_draw 一次全提交）。所以 V2/V4 也 dc≈10。这是三家中 WebGL 多纹理合批的唯一实现。

---

## 二、变换场景（V3）fps 差异的精确根因

### Laya V3 慢：每帧每精灵 6 次三角函数

**源码**：`laya.core.js` → `Matrix.setMatrix`

```js
setMatrix(x, y, sx, sy, rotation, skewX, skewY, pivotx, pivoty) {
    rotation = Utils.toRadian(rotation);
    skewX = Utils.toRadian(skewX);
    skewY = Utils.toRadian(skewY);
    const cosr = Math.cos(rotation); const sinr = Math.sin(rotation);
    const coskx = Math.cos(skewX);   const sinkx = Math.sin(skewX);   // skew=0 也算
    const cosky = Math.cos(skewY);   const sinky = Math.sin(skewY);   // skew=0 也算
    this.a = (cosr * * sinky) * sx;
    ...
}
```

**结论**：无快速路径——即使 skew 恒 0，也无条件算 3 组三角函数。V3 每帧 = 1 万精灵 × 6 次三角函数 + 3x3 矩阵重建 = **6 万次三角函数/帧**。这是纯 CPU 变换路径的工程问题，与合批无关（V3 单纹理 dc=2 已合批，却比 V1 掉 15fps）。

### Egret V3 不慢：变换烘进顶点

**源码**：`egret.web.js` → `Batcher.setTransform` + `pushImage` 角点变换

```
变换矩阵作用在顶点角点上，一次性烘进顶点数据；
渲染时顶点已带旋转/缩放，无每帧矩阵重建。
```

**结论**：变换成本是"每精灵算 4 个顶点"（一次性），不是"每精灵重建矩阵 + 三角函数"。

### Cocos V3 不慢：Batcher2D 合批时批量处理变换

**结论**：变换走 GFX 矩阵，Batcher2D 在合批提交时统一处理，无逐精灵三角函数。

---

## 三、WebGPU 是否更快的精确根因

| 引擎 | WebGPU 2D 提交方式 | 多精灵成本 |
|---|---|---|
| **Egret 自研** | 帧级 staging：整帧顶点攒 CPU 大数组，帧末**一次 writeBuffer**（`_stageV/_stageI`） | 与精灵数无关的常数开销 |
| **Cocos** | 与 WebGL 同源（GFX 抽象 + Batcher2D），无额外逐元素路径 | 与 WebGL 一致 |
| **Laya** | **逐元素状态机**：`_prepare`（`_updateMatChangeFlag` + shader 编译比较 + 几何状态 ID）每个精灵每帧跑 | 开销随精灵数线性增长，且高于 WebGL 的合批路径 |

**Laya WebGPU 在 V3/V4 反超 WebGL 的根因**（`laya.webgpu_2D.js` `drawRenderElementList`）：

```js
for (元素) elements[i]._prepare(this);  // 逐元素状态机（WebGL 没有这层）
gpuBufferMgr.upload();
for (元素) elements[i]._render(...);
```

WebGL 路径没有 `_prepare`/`_updateMatChangeFlag`，是合批驱动（`struct._handleInterData() + renderUpdate()` 后一次 `uploadBuffer()`）。WebGPU 多出来的这层逐元素 prepare，在精灵多、状态复杂时反噬性能。

---

## 四、实测数据与机制对照（BunnyMark 10000）

| 场景 | Egret WGL/WGPU | Laya WGL/WGPU | Cocos WGL/WGPU | 机制解释 |
|---|---|---|---|---|
| V1 单纹理 | 58.3/58.1 | 58.2/58.2 | 57.7/58.1 | 都合批，无差异 |
| V2 8纹理 | 38.8/**58.3** | 29.4/33.1 | 58.3/58.3 | 见下 |
| V3 变换 | 58.3/58.4 | **43.3/41.4** | 58.6/57.3 | Laya 三角函数的代价 |
| V4 12纹理 | 40.2/**58.4** | 38/29.8 | 58.2/58.1 | 见下 |

- **V2/V4**：Cocos(multi_draw) > Egret(WebGPU 纹理槽) > Egret WebGL/Laya(同纹理断批) > Laya WebGPU(逐元素 prepare)
- **V3**：Laya 独差（矩阵重建）；Egret/Cocos 变换烘顶点无此负担
- **V1**：三引擎打平（合批饱和场景，无差异空间）

---

## 五、一句话总结（可直接进报告）

**三引擎 2D 性能差异由三个独立机制决定**：

1. **合批策略**（决定 dc）：Cocos 用 multi_draw 跨纹理合批（dc≈10）；Egret 用同纹理合批（WebGL）或纹理槽（WebGPU）；Laya 普通 Sprite 完全不跨纹理合批（dc=精灵数）
2. **变换路径**（决定 V3）：Egret/Cocos 烘进顶点；Laya 每帧每精灵重建矩阵 + 6 次三角函数
3. **WebGPU 提交方式**（决定 WebGPU 是否更快）：Egret 帧级 staging（常数开销）；Cocos 同源管线；Laya 逐元素状态机（线性增长）

**Laya 2D 表现差，是三处机制叠加的系统性结果，不是单点缺陷。**
