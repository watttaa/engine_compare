# 三引擎 2D 渲染管线全方位对比

> **方法**：本地源码多轮实查（cocos-engine / LayaAirIDE 引擎库 / 自研 webgpu-dist + egret）+ 官方文档交叉印证。
> **日期**：2026-08-28 · 基线版本：Cocos 3.8.8 / LayaAir 3.4 / Egret 自研 5.4.1

---

## 一、一句话总览

| 引擎 | 2D 合批思路 | WebGPU 2D 状态 |
|---|---|---|
| **Cocos** | 统一 Batcher2D 状态机 + multi_draw 扩展 + 动态合图 | 实验性，但 2D 管线同源（GFX 抽象） |
| **Laya** | 同图集/同纹理可合批（动态图集与序列帧/Graphics 有专用路径） | 状态机驱动，逐元素 prepare 开销大；本站交叉散图基准未启用动态合图，因而未跨纹理合批 |
| **Egret 自研** | 同纹理合批（WebGL）+ 纹理槽合批（WebGPU 专属） | 自研后端，合批最强 |

---

## 二、Cocos 3.8.8 管线

### 架构：Batcher2D 统一合批 + GFX 多后端抽象

```
节点遍历 → 收集 RenderData → 状态检查 → 合批判定 → MeshBuffer 提交 → GFX 绘制
```

### 合批条件（官方文档 + 源码 batcher-2d.ts 印证）
同材质 + 同 BlendState + 同深度/模板状态 + 同纹理源/采样器 + 同 Layer 连续。

### 三个关键机制

| 机制 | 说明 | 我们的实测证据 |
|---|---|---|
| **WEBGL_multi_draw 扩展** | 一次提交多个图元，webgl-commands.ts 检测扩展后 `multiDrawElementsWEBGL` 批量提交 | **V2/V4 dc=10**——8/12 纹理也合住，这是三家中 WebGL 多纹理合批独一份 |
| **动态合图** | `ENABLE_DYNAMIC_ATLAS` 宏，小纹理运行时合并 | 我们基准未开（默认关），但 multi_draw 已够 |
| **自定义 Assembler** | 组件级扩展渲染数据组装 | — |

### WebGPU 管线
实验性（Web-Desktop 限定），但 **2D 渲染器与 WebGL 同源**（都走 Batcher2D + GFX 抽象层），所以 WebGL/WebGPU 的 dc 基本一致（V1-V4 均 dc≈10）。

### 一个引擎缺陷（选型报告可引用）
3.8.8 的 `WebGL2CommandBuffer` 从不递增 `_numDrawCalls`（源码实锤：begin() 只清零），导致 **WebGL 构建里 `device.numDrawCalls` 恒 0**——官方性能面板的 drawCall 数在 WebGL 构建不可信。

---

## 三、LayaAir 3.4 管线

### 架构：SortedStructs 分层 + 专用批处理 + 图集兜底

```
structs(按 zIndex 分层) → 逐 struct 循环 → 合批判定 → 提交
```

### 合批机制（源码 + 官方文档印证）

| 机制 | 适用范围 | 说明 |
|---|---|---|
| 同图集合批 | 普通 Sprite | 连续同图集可合批；交叉不同图集会增加 DrawCall（官方性能优化文档） |
| 动态图集 | 小纹理路径 | `AutoTextureConfig.enableAutoDynamicAtlas` 开启时，`LargeTexProcessor` 自动创建 `DynamicAtlasManager` 并纳入小纹理；本站交叉散图基准未启用此路径 |
| `SequenceFrame2DInstanceBatch` | 序列帧动画 | GPU 实例化合批（`MaxInstanceCount` 上限） |
| `WebGraphicsBatch` + `allowReorder` | Graphics 矢量图 | 按分组重排合并 |
| UBO / Mesh 预分配 | 全局 | `enableUniformBufferObject` / `webGL2D_MeshAllocMaxMem` 已开 |

### WebGL vs WebGPU 的关键差异（源码实锤）

**WebGL 路径**：`struct._handleInterData() + renderUpdate()` 循环后 `uploadBuffer()` 一次提交——**合批驱动**，无逐元素状态机。

**WebGPU 路径**（webgpu_2D.js `drawRenderElementList`）：
```js
for (元素) elements[i]._prepare(this);   // 逐元素：_updateMatChangeFlag + shader编译比较 + 几何状态ID
gpuBufferMgr.upload();
for (元素) elements[i]._render(...);     // 再逐元素 render
```

**每帧对每个精灵跑一遍 `_prepare` 状态机**——这是 WebGPU 在 V3/V4 反超 WebGL 的根因：
- V3（变换）：`_updateMatChangeFlag` 每帧真算矩阵
- V4（12 纹理）：几何状态/材质定义比较命中率最低，prepare 成本最大

### 多纹理合批：未实现
`textureArrayIndex` 在顶点管线有读取代码，但**全引擎零赋值**（源码 grep 实锤）——纹理阵列合批的架子搭了没接线（可能在 3.5 的 WebGPU Driven 路线图）。

### 运动/轨迹模型（水族馆场景）
Laya 的 2D 无内置鱼群/轨迹模型，需业务自实现。我们用统一 sim-core（每条鱼独立波浪轨迹：y0/amp/freq/phase 各异），三引擎同源对比。

### 掉帧率 100% 的代码级解读
掉帧率 = 帧时间 >16.7ms 占比（sim-core 统一口径）。Laya 在 V2/V3/V4 掉帧 100% = **每帧都超过 16.7ms**（fps 29.4~43.3 全程低于 60），与逐元素提交/矩阵重建的持续开销自洽，不是统计误差。对比 Cocos V2 掉帧 47%（fps 58.3，半数帧轻微超预算）——**掉帧率是 fps 的精细版**。

### dc=10000 的代码级根因（多纹理交叉完全不合批）

Laya 的渲染列表收集后，**每个精灵 = 一个独立渲染元素（element），逐个 `_render` 提交，无任何跨精灵合并**：

```js
// laya.webgpu_2D.js · drawRenderElementList
for (let i = 0, n = list.length; i < n; i++) {
    elements[i]._render(this, this.renderCommand);   // 每精灵一次提交
}
this._submit();
...
Laya.LayaGL.statAgent.recordCTData(StatElement.CT_2DDrawCall, list.length);
//                                                      ^^^^^^^^^^^
//                          list.length 就是精灵数 → dc 直接等于精灵数
```

合批判定只存在于专用批处理器（`SequenceFrame2DInstanceBatch` 序列帧、`WebGraphicsBatch` 矢量图），**普通 Sprite 不进任何批处理器**，走 `NullBatchProvider`（逐个 add，不合并）。所以：
- V2/V4（纹理交叉）→ dc = 精灵数 = 10000，且 WebGL/WebGPU 一样糟（都没普通精灵合批）

### 变换场景（V3）慢的代码级根因：矩阵重建无快速路径

`Matrix.setMatrix`（laya.core.js）每帧对每个精灵无条件重算**全部三角函数**，即使 skew 恒为 0：

```js
setMatrix(x, y, sx, sy, rotation, skewX, skewY, pivotx, pivoty) {
    rotation = Utils.toRadian(rotation);
    skewX = Utils.toRadian(skewX);
    skewY = Utils.toRadian(skewY);
    const cosr = Math.cos(rotation);  const sinr = Math.sin(rotation);
    const coskx = Math.cos(skewX);    const sinkx = Math.sin(skewX);  // ← skew=0 也算！
    const cosky = Math.cos(skewY);    const sinky = Math.sin(skewY);  // ← skew=0 也算！
    this.a = (cosr * cosky - sinr * sinky) * sx;
    this.b = (sinr * cosky + cosr * sinky) * sx;
    ...
}
```

V3 每帧 1 万精灵 × 6 次三角函数 + 完整 3x3 矩阵重建 = 每帧 6 万次三角函数。对比：
- **Egret**：变换矩阵**烘进顶点**（一次性），渲染时顶点已带变换，无每帧矩阵重建
- **Cocos**：变换走 GFX 矩阵 + Batcher2D 合批，顶点数据变换在提交时批量处理

这是纯 CPU 变换路径的工程问题，与合批无关——**V3 是单纹理（dc=2 已合批），却比 V1 掉 15fps，正是这条重路径**。

### 掉帧率 100% 的解读

掉帧率 = `帧时间 > 16.7ms 的帧占比`（sim-core 统一口径）。Laya 在 V2/V3/V4 掉帧 100% = **每帧都超过 16.7ms**（即全程低于 60fps），与 fps 29.4/43.3/38 完全自洽，是真实反映"每帧都慢"，不是统计误差。

对比：Egret V2 webgl 掉帧 99.7%（fps 38.8，接近全慢）、Cocos V2 掉帧 47%（fps 58.3，半数帧轻微超预算）。**掉帧率是 fps 的精细版**——fps 看平均，掉帧率看"每帧是否稳定达标"。

---

## 四、Egret 自研 5.4.1 管线

### 架构：两代合批方案 + 自研 WebGPU 后端

| 代 | 后端 | 机制 | 状态 |
|---|---|---|---|
| ① 同纹理合批 | WebGL | 纹理对象身份合并 | 引擎原生 |
| ② 动态合图 | WebGL | TextureAtlasManager 2048×2048 自动大图集 | 生产默认关（需宿主上传回调初始化） |
| ③ SDF 字形合批 | WebGL | 引擎层 sdf_text + tint 不断批 | 已上线（2026-08-25） |
| ④ 纹理槽合批 | **WebGPU** | ≤16 纹理槽，texId 进顶点，跨纹理合批 | 已实装 |

### WebGPU 后端完整优化（webgpu-dist，源码实查）

| 类 | 优化 |
|---|---|
| 合批 | 16 纹理槽、变换烘顶点、颜色走 tint、白纹理兜底、multiTex 可关 |
| 提交 | **帧级 staging**（整帧一次 writeBuffer）、状态去冗余、BindGroup 缓存 |
| 移动端 | Stencil 懒路径（无 mask 不开 depth/stencil） |
| 测量 | timestamp-query GPU 计时（60 帧一采防污染） |
| 能力 | SDF 模块（WebGPU 版）、后处理模糊、**GPU compute 群集原型**（10 万鱼三段 compute） |
| 压缩 | BC/ETC2/ASTC 块格式直传，按 adapter 能力请求 |

### 合批 vs 提交：为什么 Egret WebGPU V2/V4 大胜
纯 CPU 数组一次提交（无逐元素状态机），多纹理通过顶点 texId 选纹理——那层逐元素 prepare 开销**。

---

## 五、实测数据对照（BunnyMark · 10000 · 真机）

| 场景 | Egret WebGL/WebGPU | Laya WebGL/WebGPU | Cocos WebGL/WebGPU |
|---|---|---|---|
| V1 单纹理 | 58.3 / 58.1 | 58.2 / 58.2 | 57.7 / 58.1 |
| V2 8纹理 | 38.8 / **58.3** | 29.4 / 33.1 | 58.3 / 58.3 |
| V3 单纹理+变换 | 58.3 / 58.4 | 43.3 / 41.4 | 58.6 / 57.3 |
| V4 12纹理 | 40.2 / **58.4** | 38 / 29.8 | 58.2 / 58.1 |

### 三个核心结论

1. **引擎间差距 = 多纹理合批能力**：Cocos（multi_draw，全场景 dc≈10）> Egret（WebGPU 纹理槽）> Laya（无，dc=10000）

2. **WebGPU 是否更快取决于引擎有没有用它做合批**：
   - 做了（Egret 纹理槽 / Cocos 同源）→ 破批场景大胜或打平
   - 没做（Laya）→ 破批场景反而更慢（逐元素 prepare 反噬）

3. **V3 是重要发现**：Laya 的变换场景 WebGPU 比 WebGL 慢 5%，暴露了它 WebGPU 路径逐元素状态机的额外开销；Egret/Cocos 无此问题（Egret 烘顶点、Cocos 同源管线）

---

## 六、公平性声明

- 三引擎都在**同机、同分辨率、同资源、同采集代码**（sim-core）下实测
- Laya/Cocos 的**可开优化均已开启**（UBO、MeshAlloc、multi_draw 自动检测、customPipeline 等）
- Egret 为自研引擎，纹理槽合批/SDF/动态合图为本团队开发（利益相关披露）
- Laya 动态合图/多纹理合批**不是没开，是 3.4 没实现**（textureArrayIndex 未接线）
